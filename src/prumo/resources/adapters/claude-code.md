# CLAUDE.md

This project uses Prumo v0.2. Start with `ENTRYPOINT.md`, `prumo.json`, active Goal and `docs/PRUMO.md`. Treat Prumo as an external CLI utility in PATH (`prumo`); do not inspect framework source repositories. Follow Lean Progressive Context, bounded output and documentation-delta rules. Never treat generated summaries/context as durable truth.
