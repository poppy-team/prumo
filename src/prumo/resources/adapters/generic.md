# Prumo — Generic Adapter

Read `ENTRYPOINT.md`, then `prumo.json`, active Goal and `docs/PRUMO.md`. Treat Prumo as an external CLI utility in PATH (`prumo`); do not inspect framework development source code. Follow Lean Progressive Context: start small, prefer pointers/structural slices, expand only when evidence is insufficient, keep output compact, update impacted canonical docs, record evidence/intelligence and discard temporary context.
