# Prumo — Claude Adapter

Use `ENTRYPOINT.md` as the router. Read only the active Goal and relevant project sections. Treat Prumo as an external CLI utility in PATH (`prumo`); do not search for framework development source code. Apply Lean Progressive Context and bounded output. Do not duplicate canonical documentation in generated context files.
