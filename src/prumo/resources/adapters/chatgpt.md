# Prumo — ChatGPT Adapter

Use the repository as durable truth. Start at `ENTRYPOINT.md`; do not ask to preload the whole project. Treat Prumo as an external CLI utility in PATH (`prumo`); do not inspect framework source code. Follow the active Goal and LPC/PCA budgets. Prefer concise findings and references over repeated payloads. Stable project changes belong in canonical Markdown/JSON, not chat memory.
