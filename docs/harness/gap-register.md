# Harness Gap Register (canônico)

Single source of truth for everything missing. Authority: task DoD (§35) +
snapshot `2026-09-11-d41f18bb65f5` (págs. 18, 19, 22–27, 29–35, 40–41) +
code/tests reality. `ACCEPTED != implemented`; environmental and
approval-gated items are labeled, never disguised.

> **Como ler:** este registro diz *o que* falta e em que estado; para *o que
> cada item é*, por que ele importa e o que exatamente o destrava, veja
> `docs/harness/open-work.md` — o mesmo conteúdo em linguagem que não exige
> conhecer o código.

**Disciplina:** todo incremento atualiza os Status aqui e imprime a tabela
no relatório da rodada. IDs são estáveis — nunca reutilizar um ID para outro
item. Novos gaps entram no fim com o próximo número livre.

**Legenda:** ✅ done · 🟡 partial · ⬜ open · 🛑 blocked · ⏸️ deferred ·
🚫 out-of-scope (deste Goal).

## 1. Capability gaps

| ID | Item | Fonte | Status | Evidência / limite | Próximo passo |
|----|------|-------|--------|--------------------|---------------|
| GAP-001 | Budget envelope no path real | DoD §35, HA4 | 🟡 partial | Tracker + flags + persist existem e o CLI os liga; o **daemon não**: cria o tracker com todos os limites zero (`daemon.go:393-397`) e `Envelope` trata zero como ilimitado (`budget/budget.go:32-39`). `Reserve` grava a reserva sem descontá-la do disponível (`budget/budget.go:23-30`) e a cobrança é pós-resposta (`runlayer/runlayer.go:40-52`), então um run sem `--budget-usd` não tem teto | GAP-098 (teto no daemon) + GAP-100 (verificação pré-flight) |
| GAP-002 | Evidence/Gates do protocolo no loop | HA2/HA4, pág. 05 | ✅ done | evidence-<run>.json + `--strict` + políticas declarativas (`--gates`) | motores externos futuros |
| GAP-003 | Team binding real (Runner aninhado por role) | HA10/HA14 | 🟡 partial | `team.Runner`/`RunWork` criam runs aninhados com budget e checkpoint próprios (`team/bind.go:23-81`), mas **nenhum chamador de produção**: o único import é `harness/eval/eval_test.go:24`. CLI e daemon constroem um único `harnessruntime.Runner` (`agent_commands.go:121-315`, `daemon.go:393-445`) e nunca importam `harness/team` | GAP-101 (delegação em produção) |
| GAP-004 | Egress + segredos no path local | HA-seg, págs. 07/19 | ✅ done | redação default + EgressPolicy fail-closed c/ allowlist (`--egress-deny/--allow`) | postura legacy quando nil (explícito) |
| GAP-005 | Roteamento por custo/latência/privacidade/quota | HA6, pág. 24 | 🟡 partial | `harness/gateway` implementa seleção, cooldown, retry, fallback e circuit breaker, mas **zero consumidores fora de teste** (busca por `harness/gateway` em `*.go` exclui `_test.go`: nenhum resultado). O runner chama `r.Svc.Models.Stream` direto (`runtime/runtime.go:303`). Além disso, três defeitos no próprio gateway: `CooldownUntil` é escrito e nunca lido, então circuito aberto nunca recupera (`gateway.go:198-220`); `quotaExhausted` lê o mapa sem mutex enquanto `SetQuota` escreve com lock (`gateway.go:70-75` vs `165-169`); `LocalOnly` aceita privacy desconhecida (`gateway.go:67-69`) | GAP-102 (ligar ao runtime) + GAP-103 (corrigir circuit breaker) |
| GAP-006 | Retrieval estrutural (FTS/BM25, símbolos/LSP, repo map) | HA9, págs. 08/31 | ✅ done nos limites | BM25 + repo-map + LSP (symbols/hover/definition, fallback) | typed graph + embeddings |
| GAP-007 | Regiões gerenciadas + JSON Patch no doc compiler | HD-base, pág. 26 | ✅ done nos limites | regions + RFC6902 + seções Markdown estruturais | AST pleno (listas/tabelas) |
| GAP-008 | Merge/conflict explícito entre worktrees | HA10 | ✅ done | three-way + deleções propagadas/conflitadas, base intacta | — |
| GAP-009 | Steering + compaction | HA2, pág. 05 | ✅ done | Inject/op/CLI/SDK + CompactKeep/Budget auto + ACP bridge | — |
| GAP-010 | ACP Agent Server (expor Runtime a editores) | H11, págs. 06/22 | ✅ done nos limites | servidor ACP v1 (spec oficial) + `agent acp` + bridge testada vs daemon + cliente ACPClient no extagent (dogfood live Prumo→ACP→Prumo 2026-09-14, 0 API key) | verificação c/ cliente real (Zed) |
| GAP-011 | MCP client (transporte + integração tools) | H5 | ✅ done nos limites | stdio + HTTP c/ sessão + Adapter + Fanout + `--mcp` | SSE streams futuros |
| GAP-012 | Benchmarks (packing, compilação, Runner) | relatório | ✅ done | compile ~6.8ms, BM25 ~0.78ms, run ~7µs (i7-3632QM) | — |
| GAP-013 | Operação do daemon (PID lock, rotação, unit, stop) | daemon | 🟡 partial | Stop trava e rotação existem, mas: `AcquireLock` faz check-then-write sem `O_EXCL`/`flock`, então dois daemons passam (`daemon/supervise.go:18-35`); PID reutilizado é tratado como vivo e `Stop` envia SIGTERM para o processo errado (`:25-30`, `:47-57`); `Stop` retorna logo após o sinal, sem esperar a saída (`:48-57`); SIGTERM não é shutdown gracioso — contextos de run vêm de `context.Background()` e nada é drenado ou gravado (`daemon.go:371-386`, `remote.go:37-71`) | GAP-104 (lock atômico) + GAP-105 (shutdown gracioso) |
| GAP-014 | Kill -9 real com side effect pendente | HA4/HA8 | ✅ done | TestDaemonKillRecovery: SIGKILL + takeover + store íntegro | kill mid-side-effect em CI |
| GAP-015 | Approvals persistidas | HA3/pág. 07 | 🟡 partial | `SavePermissions` grava `permissions-<run>.jsonl` (`runlayer/runlayer.go:147-197`), mas **nada o recarrega**: as decisões vivem só no mapa em memória do `perm.Engine` (`perm/perm.go:25-39`, `102-109`) e `Evaluate` não registra as decisões de política automática, só as de `Approve`/`Deny` (`:42-109`). Um restart de daemon não consegue continuar uma aprovação pendente. Além disso a identidade é o `tool_call_id` do modelo, sem comparar ação/recurso/argumentos (`:42-48`) | GAP-106 (recarregar decisões) + GAP-107 (aprovação por fingerprint) |
| GAP-016 | Bridge AgentEvent → observability.Event | HA4 | ✅ done | obs-<run>.jsonl dual-write CLI+daemon | — |
| GAP-017 | Planning→Build (PlanningSession ⇒ Run) | pág. 25 | ✅ done | handoff/promote.go + `agent promote [--start]` | — |
| GAP-018 | Memory Atlas | págs. 25/27-G12 | ✅ done nos limites | Atlas local + recall + Promote c/ gates (restricted/confidential nunca cruzam) | freshness/TTL automáticos |
| GAP-019 | Agent writes KnowledgeDelta-first (G15) | pág. 27-G15 | ✅ done | seeding via Commit com Author | — |
| GAP-020 | Retention/GC (checkpoints, eventos, knowledge) | pág. 27-G23 | ✅ done | RetentionPolicy + GC (prune 5, órfãos 30d, provenance intacta) + `agent gc` | política por projeto futura |
| GAP-021 | Retry com backoff no Gateway | HA6 | 🟡 partial | As classes de rate limit e o jitter existem (`gateway/gateway.go:126-140`), mas o gateway não é chamado em produção (ver GAP-005) e o próprio backoff é **linear** com jitter determinístico, divergindo do contrato de exponencial+`Retry-After` (`control-plane.md:44-58`). `isRateLimit` não reconhece `RESOURCE_EXHAUSTED` (`gateway.go:316-327`) | GAP-102 + GAP-108 (exponencial + Retry-After) |
| GAP-022 | Child runs/subagentes com ownership (H14) | H14 | 🟡 partial | `RunWork` cria `NativeAgent` aninhado com checkpoint e budget próprios (`team/bind.go:23-81`), mas sem chamador de produção (ver GAP-003). Não existe a ferramenta `agent.delegate`/`agent.ask` que `harness/specs/ch06.md:13-24` especifica, nem operação de delegate/spawn no protocolo do daemon (`harness/protocol/manifest.go:6-47`). `RunWork` aceita `PhaseYield` como sucesso (`:72-81`), então um filho cancelado conta como concluído. `may_delegate` não tem uso em Go e `max_delegation_depth` é só configuração | GAP-101 + GAP-109 (fingerprint de profundidade fixo) |
| GAP-023 | Scheduled runs (H16) | H16 | ✅ done | retry linear + dead-letter + last-status | supervisão externa (systemd doc) |
| GAP-024 | Provedores sandbox adicionais (H13) | H13 | 🟡 partial | StrongProvider detect + `--sandbox-runtime` (ex. runsc) | execução verificada + remota |
| GAP-025 | Contratos Local Intel (KnowledgeTask, Router, ResourceManager, Supervisor) | págs. 29–30 | 🟡 partial | tipos + router + supervisão c/ backoff + detecção llama-server | workers de inferência + benchmarks |
| GAP-026 | Research Ledger first-class (G11) | pág. 27-G11 | ✅ done | Add/Resolve/Open Delta-first | — |
| GAP-027 | Token-estimate index (G18) | pág. 27-G18 | ✅ done | tabela tokens-v1 + uso no Context | calibração medida |
| GAP-028 | Lint dos agent docs + doc-evals (G19/G21) | pág. 27 | ✅ done | humandocs.Lint (presença/fiação/higiene) | lint de agent-docs legados |
| GAP-029 | Schema evolution/migrations (G20) | pág. 27 | ✅ done | schemareg (parse-all + Migrate por versão) | migrações quando houver v2 |
| GAP-030 | Transporte remoto do daemon (+auth) | split gate | ✅ done nos limites | TCP+TLS + token (subtle), SDK+CLI, teste live local | CA corporativa + hardening de exposição |
| GAP-031 | Decisão: MCP Go SDK e transports | pág. 19 | ✅ decided | stdlib JSON-RPC registrado em mcp.go (troca sem mudar superfície) | reavaliar com benchmark |
| GAP-032 | Decisão: subset ACP + matriz oficial | pág. 19 | ✅ decided | subset v1 registrado no código (init/new/load/resume/list/delete/close/prompt/cancel); cliente stdio implementado (ACPClient) cobrindo o mesmo subset | registry/mCP-per-session futuros |
| GAP-033 | Decisão: Docker vs Podman padrão/rootless | pág. 19 | ✅ decided | ADR 006: podman-preferred na detecção, docker como runtime de referência testado (GAP-037), rootless obrigatório quando disponível, fail-fast sem fallback silencioso | medir podman rootless quando houver ambiente |
| GAP-034 | Decisão: driver SQLite derived runtime | pág. 19 | 🟡 partial | ADR 007 (Proposed): modernc.org/sqlite puro-Go atrás da port DerivedIndex; cgo só opt-in | medir workload real do primeiro componente SQLite para Promote |
| GAP-035 | Decisão: isolamento de plugins | pág. 19 | ✅ decided | ADR 008: sem loader in-process; extensões out-of-process via contratos existentes (MCP/ACP/CLI providers); plugin futuro = processo próprio com handshake tipado | YAGNI até uso real |
| GAP-036 | Decisão: budgets de performance + TTL/memory-pressure | pág. 19 | 🟡 partial | ADR 009 (Proposed): política de baselines medidos agora; thresholds só de distribuições medidas; TTL default 7d + prune-on-start | thresholds após primeiro componente SQLite (ADR 007) |
| GAP-037 | Prova live do container | HA5 | ✅ done | TestContainerLive PASS 2026-09-14 (docker, alpine:latest, limits + rede negada) | — |
| GAP-038 | Chaves live de models | HA6 | 🛑 env | adapters prontos + stub-testados | `PRUMO_MODEL_API_KEY`/BASE_URL |
| GAP-039 | Sends live externos (opencode/codex/cursor) | HA7/HA8 | ✅ done nos limites | spend aprovado: cursor-agent turno real PASS 2026-09-14 (auth própria, 0 API key); opencode Send reach server mas OpenRouter s/ saldo e opencode/* free em 429 (rate limit vendor); codex ChatGPT em usage limit até 19/09; caminho CLI opencode run também validado até o boundary (mesmo 429 vendor em mimo-v2.5-free) — bloqueio é 100% vendor-side, adapters e protocolo OK | re-testar opencode/codex quando quota voltar (após 19/09) |
| GAP-040 | Modelos locais + thresholds (benchmark-driven) | pág. 19/29 | 🛑 env+decision | No-LLM first-class mantido | hardware + corpus + aprovação |
| GAP-041 | Bindings não-Go (TS types do IDL) | HA11 | ✅ done nos limites | protocol.d.ts + client.ts c/ roundtrip live vs daemon Go | mais linguagens sob demanda |
| GAP-042 | Checkpoint retention no daemon store | HA4 | ✅ done | coberto por GAP-020 (mesmo store) | — |
| GAP-043 | Descoberta de modelos nos adapters reais | HA1 | ✅ done nos limites | OpenAI lista `/models`; Anthropic não expõe listagem (limite do vendor). A descoberta virou alcançável pelo protocolo em 2026-09-17: op `models` (0.3.0) sobre `ModelProvider.Models`, com CLI, SDK Go/TS e teste — a lacuna que tinha removido o seletor de modelos do cliente | — |
| GAP-044 | Structured-output enforcement | HA1 | ✅ done | validador subset + enforcement nos adapters + response_format | subset documentado |
| GAP-045 | Impact analysis lexical (G5, legado M5) | pág. 27-G5 | ✅ done | matchers tipados path:/ext:/contract:/rel: + legado rotulado; entrada antiga preservada | — |
| GAP-047 | Critérios 4 e 5 do H10 não medidos | pág. 13/H10 | ✅ done | Critério 4: `prumo-agent tui --remote/--token/--token-file/--remote-tls-cert` monta o **mesmo** cliente SDK em outro endereço (não um segundo caminho de código) e não supervisiona daemon local; `TestLiveRemoteDaemonFlow` roda palette→goal→stream→**aprovação**→evidência sobre TCP+TLS+token e confirma que token errado é recusado. Critério 5: baseline medido na hardware de referência do ADR 009 (i7-3632QM), janela de 200 rows — `View` 4,38 ms/frame (83 KB, 1846 allocs), `Timeline.Append` 146 µs/op (2 allocs), `Session.Poll` com log cheio e cursor avançado 45 µs/op (1 alloc). **Nenhum threshold inventado**: os números estão no ADR 009, como a metodologia exige, e o critério 5 era explicitamente não-bloqueante | — |
| GAP-046 | Op de aprovação no protocolo (H10 critério 2) | pág. 13/H10 | ✅ done | Protocolo 0.2.0 ganha `approve`/`deny` (`run_id` + `request_id`). Três defeitos encadeados foram consertados: (1) `perm.Engine.Evaluate` consulta a decisão gravada antes da política — antes `Approve` era registrado na trilha e **ignorado**, então aprovar não mudava nada; (2) o yield para aprovação persiste checkpoint com `pending_tools`/`pending_permissions` (o schema do checkpoint não descrevia nenhum dos dois) e o evento `permission_wait` passa a nomear o `request_id`, sem o qual o cliente não tem o que responder; (3) o daemon mantém o run vivo em `awaiting_approval` e reexecuta o turno em vez de descartar o runner — `yielded` (run parado) e `awaiting_approval` são estados distintos, e confundi-los faria o cliente ou travar ou sair do painel cedo. Superfície: `agent approve\|deny` + `agent ps` nomeando o pendente, SDK Go/TS, e a TUI (`a`/`d` no painel de run). Provas: `TestDaemonApprovePermission`, `TestDaemonDenyPermission`, `TestAgentApprovePendingPermission`, `TestVerticalSliceFlow` e `TestLiveRemoteDaemonFlow` (aprovação sobre TLS+token) | — |
| GAP-050 | Inventário de interface com posição e interligações | W5/W22, págs. 13/24 | ✅ done | `ui-component-contract` existia desde W5 e **nenhum produtor existia**: as quatro obrigações (`ui.component-contracts`, `ui.information-architecture`, `ui.screen-inventory`, `ui.layout`) estavam satisfeitas por waiver, e o gate ficava verde enquanto nenhum inventário de componentes existia. Agora: `docs/ui-ux/interface-map.json` (24 elementos, 10 interligações tipadas) + `internal/uimap` + `prumo-agent ui map\|verify\|impact`, com projeções para desenvolvedor, site e code agent. Declarado vence, derivado preenche, divergência é erro (ADR 012) | — |
| GAP-048 | Categorias ocultas na ajuda de topo | CLI | ✅ done | `helpCategoryOrder` não listava `Harness`, `Adoption & Migration` nem `Platform & Automation`, então `agent`, `tui`, `adopt` e `automation` **nunca apareciam** em `prumo-agent help` — inclusive `agent`, a porta de entrada do harness. Um comando que ninguém descobre não existe. Ordem corrigida e `TestEveryCommandCategoryIsPrinted` falha se alguma categoria do registry deixar de ser impressa | — |
| GAP-049 | Projeção de tokens: resolução no-op e no-color incompleto | Stack H | ✅ done | Dois defeitos da mesma família ("uma afirmação que nenhum gate verificava"). `Palette.resolveReferences` tinha receiver por valor e remapeava o mapa, então a resolução era **descartada** e todo consumidor lia referências cruas (`token:color.panel`); passou despercebido porque o único tema com valor literal era o `no-color`. E `theme.no-color` neutralizava 4 de 10 tokens de cor: o tema prometia monocromia e ainda pintava. Corrigido com receiver por ponteiro e `validateNoColorIsComplete`, que falha fechado quando um tema suprime a cor pela accent mas deixa outros tokens de cor resolvidos | — |
| GAP-051 | Nenhum evento do cliente chegava ao program | ADR 013 | ✅ done | O cliente publicava nos brokers e **ninguém assinava**: `Runner.observe` publicava `agent.AgentEvent`, `permission.Service.Raise` publicava o gate, e o modelo Bubble Tea tratava `pubsub.Event[...]` em casos que nunca disparavam — o canal devolvido por `Run` era descartado, então nenhum texto, tool call ou pedido de aprovação chegava à tela. Agora `internal/stream` assina os cinco brokers e empurra para o `tea.Program` (ligado no `main`), o envio deixou de bloquear o loop de update (virou `tea.Cmd`), e `Runner.emit` é o único ponto de entrega — inclusive para erros, que antes só iam para o canal que ninguém lia. Provas: `internal/stream/stream_test.go` (4 casos + cancelamento) e `internal/runtime/streaming_test.go` | — |
| GAP-052 | Reuso do id da sessão como run id sem cursor de fold | ADR 013 | ✅ done | O log de um run é append-only e o cliente usa o id da sessão como run id: a segunda mensagem re-dobrava a timeline inteira, redesenhando a primeira resposta e **contando o uso duas vezes**. Agora o fold retoma de um cursor por sessão e um log reescrito (rotação, rotação/restart do daemon) re-deriva o gasto em vez de somar. Um segundo defeito apareceu no mesmo caminho: a mensagem do assistente era gravada com `CreatedAt=0` e, como o store ordena por data, as respostas subiam acima das perguntas. Provas: `TestASecondRunResumesTheFold`, `TestRewoundLogIsReDerived`, `TestRunOutputBecomesAMessage` | — |
| GAP-053 | Mapa e matriz descreviam o spike arquivado, não o cliente | ADR 013 §Consequences, W22 | ✅ done | `docs/ui-ux/interface-map.json` verificava `archive/tui/**` e a matriz declarava a superfície do spike: o cliente que embarca não tinha inventário nem vocabulário de estados — o passo 4 da sequência do ADR 013. Agora o par canônico descreve `prumo-agent tui` (27 elementos, 10 arestas, símbolos provados contra `prumo-agent tui/**/*.go`) e o par do spike vive em `archive/tui/` com nota de arquivamento; `prumo-agent ui verify` passa com as projeções regeradas. As 10 claims do `bindings.json` que citavam a estrutura antiga foram reescritas junto | — |
| GAP-054 | `interaction.md` ainda é a spec do spike | W5/`ui.interaction` | ✅ done | A keymap canônica descrevia outro cliente — `Ctrl+P` (paleta), `Ctrl+T` (árvore de arquivos), `Ctrl+R` (re-run), um timeline com descarte — e nada comparava documento e código: a deriva durou o que durou até alguém ler os dois. Agora `docs/ui-ux/interaction.md` descreve o cliente que embarca (composer-first, sem mouse, foco com glifo) e `TestDocumentedKeymapMatchesTheBindings` verifica a tabela **nos dois sentidos** — um chord que o cliente responde sem linha, e uma linha sem binding, falham. Ao ligar o teste ele achou 17 divergências (chords de lista, do filepicker, das ações dos diálogos) que passaram a estar documentadas. A spec do spike vive em `archive/tui/interaction.md` | — |
| GAP-055 | CI não alcançava o módulo do cliente | ADR 013 §Consequences | ✅ done | `go test ./...` na raiz pula módulo aninhado: `prumo-agent tui` não era testado, vetado nem compilado em CI, e o workflow fixava Go 1.22 enquanto os dois `go.mod` exigem 1.26.6 (a toolchain era baixada em silêncio). Agora existe o job `tui-test` (vet, `go mod verify`, `go test -race`, boundary, build) e o setup-go lê `go-version-file` | — |
| GAP-056 | Redraw do transcript é O(n) por mudança de largura | ADR 009, §24.3 | ✅ done | Medido com `BenchmarkRenderConversation`/`BenchmarkResizeStorm`: um frame com cache quente custa ~29 ms e ~8 MB de alocação tanto com 200 quanto com 2000 mensagens (o viewport limita o desenho), mas **uma mudança de largura re-renderiza o transcript inteiro** — 3,1 s por passo com 2000 mensagens, porque cada turno passa pelo markdown outra vez. Uma mudança só de altura já não invalida nada. Arrastar a janela num run longo era o caso que doía — fechado por GAP-079 (um render por arrasto) e GAP-080 (renderiza a cauda, não a conversa): primeiro paint 488 ms → 45 ms e resize 3,1 s → 172 ms | — |
| GAP-057 | Uso de tokens: o cliente só mostrava zero | ADR 013 | ✅ done | A statusline mostrava `Context: 0, Cost: $0.00`: a sessão só recebia id/título, e o harness consumia `EventUsageUpdated` no budget **sem publicá-lo na timeline**. Agora o runtime emite `usage` (`prompt_tokens`, `completion_tokens`, `cost_usd`) e o cliente soma o que lhe foi dito. O percentual de contexto não é inventado: sem janela declarada pelo harness, a linha mostra o gasto e nenhuma fração (`TestUsagePercentIsNotInvented`) | op `models` devolver a janela de contexto, se o cliente for mostrar fração |
| GAP-058 | Estados que o cliente aplica e não desenha | W12/reconnect-replay | ✅ done | Três estados aplicáveis não tinham superfície. **`reconnecting`/`offline`**: o poll que falhava **matava o run na primeira falha**; agora há retry com contagem visível (`reconnecting k/8`), a ligação recuperada é reportada como `live`, e só depois de 8 tentativas o cliente diz `offline` e encerra o run — um retry silencioso seria indistinguível de um travamento (`TestALostDaemonIsRetriedThenCalledOffline`, `TestARecoveredLinkIsReportedLive`). **`permission-denied`**: o daemon reporta o run como falho e, do protocolo, uma negação é indistinguível de qualquer outro stop — o cliente é o único lugar onde a diferença existe, então é ele que diz (`Denied <tool>. The run stops.`; `TestDenyingReportsTheDecision`). Frames: `offline`, `reconnecting`, `permission-denied` | — |
| GAP-062 | Indicador de foco ausente | `ui.interaction` §Focus | ✅ done | `styles.FocusedBorder` existia e **nada o usava**: o composer desenhava a borda normal sempre, então foco não era visível — e o contrato exige um indicador que sobreviva a terminal sem cor. Agora `layout.Container` sabe se tem o foco e troca o **glifo** da borda (`━` focado, `─` não) além da cor, e o shell decide o foco num único lugar (um diálogo aberto tira o teclado do composer). Provas: `TestTheComposerShowsWhetherItHoldsTheKeyboard`, frame `default` com a borda pesada | — |
| GAP-059 | Custo do fold: um snapshot por evento dobrado | ADR 009 | ✅ done | `observe` publicava um snapshot por evento: um poll que absorvia 5k deltas copiava o turno 5k vezes para quadros que ninguém vê, já que a view redesenha no ritmo do poll. Agora o lote é dobrado inteiro e publicado uma vez. Medido: 20k eventos saíram de 1,93 s/1,33 GB para 0,33 s/82 MB (`BenchmarkFoldTimeline`) | — |
| GAP-061 | Gate do mapa de interface vermelho em checkout limpo | W22 | ✅ done | `prumo-agent ui verify` confere as projeções em disco, e as projeções são artefatos derivados que vivem em `.prumo/runtime/` — que o `.gitignore` exclui. Num clone limpo elas não existem, então o passo de CI **só podia falhar**, e nada nos passos anteriores as reconstruía (verificado removendo o diretório e rodando a sequência do workflow). O passo passou a construir antes de verificar (`ui map --write` + `ui verify`), o que também diz o que o gate é: uma verificação do mapa — esquema, vocabulário, posições, símbolos — e não de um diretório que ninguém versiona | — |
| GAP-063 | O código afirmava que a op `models` não existia | ADR 013 | ✅ done | Dois comentários diziam "the harness exposes no models operation, so the key has nothing to open" — mas a op existe desde a GAP-043 e o `ctrl+o` funcionava. Uma afirmação falsa dentro do código é pior que ausente: quem lê acredita. Trocados pelo que é verdade (o picker pergunta ao harness, o catálogo é do provider) | — |
| GAP-064 | Tema configurado nunca era aplicado nem persistido | `ui.theming` | ✅ done | Dois defeitos: `--theme` era aceito e **ignorado** (nada lia `config.Theme`), e `UpdateTheme` dizia persistir e só mexia em memória. Agora o tema é aplicado onde a interface é montada (`tui.New`), a escolha é gravada em `$XDG_CONFIG_HOME/prumo-agent tui/config.json` (`PRUMO_TUI_CONFIG_DIR` para testes) e lida antes das flags, e um nome desconhecido é reportado mantendo o tema em uso. Provas: `TestTheConfiguredThemeIsTheOneDrawn`, `TestAnUnknownThemeKeepsTheCurrentOne`, `TestTheChosenThemeSurvivesARestart`, `TestOnlyTheClientsOwnPreferenceIsWritten` | — |
| GAP-065 | Política de motion | W9/W11, `accessibility.motion` | ✅ done | O cliente animava sem que nada dissesse o que a animação carrega nem o que acontece sem ela. Agora: `--reduced-motion`/`PRUMO_REDUCED_MOTION` param o spinner (o verbo fica) e o piscar do caret; a política está em `docs/ui-ux/interaction.md` §Motion, e `TestReducedMotionStatesProgressWithoutAnimatingIt` exige que os dois frames difiram **só** na linha que anima. Ela também revelou que um frame de estado animado não é capturável — os frames `active`/`partial` são capturados com a animação desligada | — |
| GAP-066 | Copy de erro sem forma definida | `accessibility.cognitive-clarity` | ✅ done | As falhas apareciam como o erro cru (`no harness attached`), sem dizer o que o cliente tentava fazer nem o que fazer depois. Agora `util.ReportFailure(operação, próximo passo, err)` impõe a forma em todos os pontos que o cliente levanta (iniciar run, reattach, listar runs, responder gate, escolher modelo, exportar), o texto do erro segue **inalterado** — o cliente cerca o registro, não o reescreve — e a política está em `docs/ui-ux/interaction.md` §Error copy. Provas: `errorcopy_test.go` (forma e ordem), `TestAGateIsAnnouncedInWords` | — |
| GAP-067 | Aprovação ilegível fora do diálogo e histórico não exportável | W11, §24.3 | ✅ done | Um gate que só existe como diálogo não pode ser lido de um frame nem por um script: agora a statusline anuncia `Permission required: <tool> — a to allow, s for the session, d to deny`, e o frame `permission-requested` o mostra. E a paleta ganhou "Export the session's timeline", que escreve o registro do run (lido do daemon, não do que o cliente dobrou) em `.prumo/runtime/exports/<run>.txt`, em texto puro, com chaves ordenadas para duas exportações do mesmo run serem idênticas. Provas: `TestAGateIsAnnouncedInWords`, `TestTheTimelineExportsAsPlainText` | — |
| GAP-068 | `LICENSE.opencode` referenciado e ausente | ADR 013 §6 | ✅ done | `README.md` e `THIRD_PARTY_NOTICES.md` citavam "the upstream `LICENSE` text is preserved verbatim inside the derived module at `prumo-agent tui/LICENSE.opencode`" e o arquivo **não existia** — a proveniência estava afirmada e não guardada. Criado com o texto MIT e a linha de copyright que o próprio aviso já registrava (`Copyright (c) 2025 Kujtim Hoxha`), sem inventar detentor nem revisão | — |
| GAP-069 | Diálogos não redimensionados | W11 | ✅ done | `WindowSizeMsg` chegava a nove superfícies e não ao picker de modelos, ao de temas, ao de saída nem ao de completion: eles desenhavam para o terminal anterior até serem reabertos. Agora todos recebem o tamanho, e um comentário diz por que a lista não pode ser parcial | — |
| GAP-070 | Contraste nunca foi medido | W9/W11, `accessibility.contrast` | ✅ done | Os temas declaravam legibilidade e ninguém tinha calculado uma razão: a medição (WCAG, por par de tokens) mostrou **8 dos 9 temas** com texto secundário entre 2.3:1 e 3.3:1 e 3 temas com a cor de erro abaixo de 4.5:1 — texto que existe para ser lido. Corrigido carregando cada token **em direção à cor de texto do próprio tema** até o piso (a mesma cor, mais apagada — não uma cor nova), com a regra escrita ao lado de cada valor. `TestEveryThemeMeasuresItsContrast` falha abaixo do piso; `TestContrastEvidenceIsReported` imprime a razão de cada par; uma isenção (borda não-focada) está declarada **com a razão**, não em silêncio |
| GAP-071 | Custo do frame e do primeiro render | ADR 009, §24.3 | ✅ done | Medido: `View` custava ~29 ms e ~8 MB por frame, e o primeiro render de 2000 mensagens **~1,5 GB de alocação**. Duas causas encontradas e corrigidas: o transcript era re-estilizado por inteiro a cada frame (agora só as duas linhas pequenas são) e `GetMarkdownRenderer` construía **um parser de markdown e um sanitizador por mensagem por render** (agora memoizado por tema+largura, com teto). Resultado: renderView 1484 MB → 662 MB, frame ~8,3 MB → 6,2 MB e 341k → 253k allocs. O que resta é O(n) por mensagem (o primeiro render de 2000 mensagens ainda custa segundos) e está em GAP-056 | — |
| GAP-072 | Painel de arquivos alterados | W5/`ui.screen-inventory` | ✅ done | A statusline mostrava a contagem e nada dizia **quais**: `ctrl+g` abre o painel com caminho e operação, lidos do registro do run quando abre (não é uma segunda lista que pode divergir da contagem), com estado vazio que diz o que o preencheria. Declarado no mapa (`node:files-dialog`) e na keymap, com `TestDocumentedKeymapMatchesTheBindings` amarrando os dois |
| GAP-073 | Steer não existia no cliente | HA2, `sdk/prumo` | ✅ done | Enviar com um run em curso era recusado ("Agent is working, please wait..."): a op `steer` existia no protocolo e no SDK desde sempre, e o cliente não a usava. Agora uma mensagem durante um run **fala com o run** (e entra no transcript, porque faz parte da conversa), a dica da tela diz isso em vez de deixar descobrir tentando, e a paleta continua começando runs novos quando não há nenhum em curso. Provas: `TestSteeringSaysItToTheRunningRun`, `TestSteeringWithoutARunIsRefused` |
| GAP-074 | Nenhuma saída sem terminal | W11/§24.3, `accessibility.screen-reader` | ✅ done | O cliente só existia como TUI: nada para um script e nada para um leitor de tela. Agora `--prompt "<goal>"` roda o goal e **imprime** (`--plain` em prosa, `--json` um objeto por fato), com anúncios como **deltas e não repaints** (a resposta cresce e é impressa uma vez, nunca re-lida), um gate recusado em voz alta com o motivo, e as mesmas garantias no modo interativo. A estratégia está em `docs/ui-ux/interaction.md` §Screen readers. Provas: `internal/headless` (6 testes, incluindo o caminho ponta-a-ponta). **Falta a atestação manual com um leitor de tela real** — não é algo que se possa afirmar por teste |
| GAP-075 | `ui.theming` e `accessibility.contrast` sem artefato | W9/W20 | ✅ done | O tema era uma preferência sem contrato escrito: agora `docs/ui-ux/theming.md` declara a **matriz de compatibilidade** (o que um tema deve definir), a **herança de tokens** (adaptativo por modo, nomes semânticos), o alto contraste, o no-color e o reduced motion, e os dois contratos saíram de `not-applicable` com claims e evidência de documento **e de teste**. O conjunto de tokens ganhou 2 contratos fechados (motion e cognitive-clarity já tinham sido; agora são **6 de 15**) |
| GAP-076 | Agendamento invisível no cliente | HA2/`sdk/prumo` | ✅ done | O daemon repetia runs (`schedule`/`unschedule`/`jobs` existem no protocolo desde H10) e o cliente não mostrava nada: quem agendava pela CLI não tinha onde ver o que estava agendado nem parar. Agora a paleta tem *Scheduled runs*: lê a agenda **quando abre** (o dono é o daemon, e um espelho do cliente seria uma fila que ele não mantém), mostra intervalo e próxima execução em linguagem legível, `d` para o selecionado e `esc` para fechar, e o estado vazio diz como criar um. Declarado no mapa (`node:jobs-dialog`). Provas: `jobs_test.go` (abre, lista, para, reporta falha) |
| GAP-077 | `ui.product-ux` e `ui.personalization` sem artefato | W20 | ✅ done | Os fluxos existiam como hábito e a personalização como uma linha de código. Agora `docs/ui-ux/flows.md` inventaria 7 fluxos com **caminho de falha em cada um** (um fluxo sem falha é um fluxo que deixa gente presa) sobre 6 objetivos, e `docs/ui-ux/theming.md` §Personalization responde as seis perguntas do contrato (superfície, defaults, persistência, import/export, valor inválido, limitações de plataforma). Os dois contratos saíram de `not-applicable` — **9 de 15 fechados** |
| GAP-078 | Referências a arquivos: suporte multimodal | HA1/`agent.Message` | ✅ done | Desbloqueado e implementado em GAP-090: `agent.Message` recebeu partes (`ContentPart`) opcionais e aditivas, adapters codificam imagens em base64, e referências `@` no prompt são resolvidas sob gate da capacidade `vision` declarada |
| GAP-082 | Superfície de comandos: decidida e implementada | ADR 013, `ui.product-ux` | ✅ done | "Comandos custom" estava parado por falta de decisão e o dialog de multi-argumentos era **inerte** (produzia `Submit`/`Args` e o handler ignorava). A decisão, tomada e documentada: a superfície é **do usuário**, um diretório de prompts em markdown (`$XDG_CONFIG_HOME/prumo-agent tui/commands/*.md`), porque um comando é um prompt que o autor possui — impor uma superfície de projeto seria o cliente decidindo o que a documentação do framework não decidiu. Front block opcional (`title`, `description`, `args`), placeholders `{{nome}}`, e um comando com `args` abre o dialog existente, que agora **envia** o prompt preenchido. Um placeholder não preenchido fica visível em vez de virar silêncio. Provas: `internal/commands` (5 testes) + `usercommand_test.go` (5) |
| GAP-086 | Cliente tentava subir um segundo daemon | ADR 013 | ✅ done | Achado no primeiro teste ponta-a-ponta com os binários instalados: um daemon deixado por um run anterior fazia o cliente **lançar outro**, ouvir "error: daemon already running" na cara de quem não fez nada de errado e só então (por sorte) usar o que já existia. Agora o cliente **pergunta primeiro** se há daemon naquele socket e só sobe um se não houver — mais barato que lançar um processo para descobrir |
| GAP-088 | opencode: construído e não plugado | GAP-039, HA7 | ✅ done | Resolvido por GAP-093: plugado como provider `--provider opencode` com execução delegada (ferramentas e autenticação do opencode), protocolo lido do binário instalado e medição de uso/cache/custo reportada na timeline |
| GAP-090 | Anexo de imagem no run (resolução de referências) | GAP-089, HA7, `ui.product-ux` | ✅ done | Referências a arquivos de imagem (`@caminho/imagem.png`) no prompt são resolvidas pelo runtime quando o modelo declara `vision` (GAP-089). `agent.Message` ganhou `Parts []ContentPart` (aditivo, mantendo `Content` string para compatibilidade retroativa e checkpoints); o runtime lê o arquivo, valida mime type e teto explícito de 10 MB (rejeição com erro descritivo em vez de truncamento ou envio silencioso); adapters Anthropic e OpenAI-compat codificam blocos de imagem em base64. Se o modelo não declara `vision` ou o arquivo não existe, a referência permanece no texto sem erro. Provas: `images_test.go` (5 testes), `anthropic_test.go` (payload multipart), `model_test.go` (payload openai-compat) |
| GAP-093 | opencode plugado como provider de modelo | GAP-024/B, ADR 013 | ✅ done | O opencode estava integrado e **inalcançável**: o daemon só conhecia `fake|fake-tools|openai-compat|anthropic` e o único caminho era `agent probe --opencode-url`, que testa e não roda. Agora `--provider opencode` executa um turno **delegado**: o opencode roda com as ferramentas, a política e a **autenticação dele** (inclusive os modelos gratuitos), e o Prumo recebe a resposta e o gasto. A diferença é declarada, não escondida: `Capabilities().ToolCalls=false`, porque as ferramentas são dele — encaminhar as chamadas faria o Prumo executar o que já foi executado. Forma do protocolo (`opencode run --format json`) **lida do binário instalado** (1.18.31), não adivinhada; evento desconhecido é reportado como desconhecido; erro do provider sobe com as palavras dele (quota esgotada diz que esgotou). Provas: `opencode_test.go` (8, com binário falso: texto, usage+cache+custo, tool não reivindicada, erro verbatim, saída falha, linha estranha, sem goal, discovery/health) + run delegado ponta a ponta pelo daemon |
| GAP-096 | Corrida no modo headless (dois escritores no mesmo printer) | ADR 013, `ui.machine-output` | ✅ done | Achado pelo `-race` no fecho da rodada: o `Printer` do modo headless recebe de **duas** origens — a ponte que encaminha os eventos do cliente e o próprio fim do run — e só uma delas passava pelo lock. O sintoma não é só o detector: um stream JSONL podia sair com **meia linha** de cada vez. Corrigido com uma porta própria para cada origem (`Send` para a ponte, `Spend` para o fim do run, ambas sob o mesmo lock) e a chave de deduplicação passou a incluir os tokens de cache (uma sessão que ganhou cache é um número novo, não uma repetição). Prova: `race_test.go` (2, com 16 goroutines concorrentes) — o teste falha sem a correção, e o pacote rodou 8× com `-race` sem detectar nada |
| GAP-095 | Pesquisa de capacidades de modelos (assistida por modelo) | HA7, `docs/security/trust-model.md` | ⬜ open (desenho) | Ideia do usuário: ao registrar um modelo (api-key, ACP…), um modelo barato/gratuito pesquisaria na web o que aquele modelo oferece e atualizaria nossos registros. **É possível e o encaixe já existe** (`.prumo/models.json` alimenta `model_info`, e o runtime já tem rede + ferramenta de fetch). Duas coisas precisam ser decididas antes, e nenhuma é código de cola: (1) **procedência** — o resultado é uma *afirmação sobre um modelo* vinda de fonte não conferida: tem de ser gravado com fonte, data e quem pesquisou, em arquivo próprio, com o declarado à mão **sempre vencendo** o pesquisado no merge; (2) **confiança** — o texto pesquisado é entrada externa (uma página pode conter instruções), então gravar direto é um caminho de injeção: a pesquisa deve **propor** e exigir confirmação explícita, nunca escrever sozinha. Recomendação registrada em `docs/harness/open-work.md` §9 |
| GAP-094 | Primeiro uso: oferecer um caminho que funcione | B, `ui.product-ux` | ✅ done | Quem instala sem opencode não tem como adivinhar, olhando uma lista de modelos vazia, que existe um caminho **sem api-key**. Agora o cliente verifica no start e, **só quando não há opencode e nenhum provider escolhido**, oferece três opções com a consequência escrita em cada uma: instalar o opencode (automático, `npm install -g opencode-ai` — npm e não um `curl | bash`, porque o pacote e o binário são verificáveis nesta máquina), configurar api-key (as variáveis exatas, sem escrever segredo em arquivo) ou "agora não". A resposta é gravada: perguntar de novo a cada start é importunar, não ajudar. E a resposta escolhida é a única decisão de provider que o cliente grava — um `--provider` continua sendo da invocação. Provas: `onboard_test.go` (6) + `onboard_test.go` do diálogo (4) + config (2) |
| GAP-092 | Sandbox sumia quando o podman era instalado | ADR 006 | ✅ done | Achado no mesmo dia em que o podman foi instalado (6.1.2, rootless, Arch): o probe de disponibilidade perguntava a **todos** os runtimes `info --format {{.ServerVersion}}` — e `ServerVersion` é campo do **docker**. `podman info` falha nesse template, então o podman era declarado inalcançável e a detecção, que escolhia o primeiro runtime **presente**, derrubava o sandbox de uma máquina que ainda sabia rodar containers (`TestContainerLive`: PASS → SKIP). Corrigido: cada runtime é perguntado na sua própria língua (`{{.Version.Version}}` no podman) e a detecção percorre a ordem de preferência até um **responder**. Medição: `TestContainerLive` passa com podman rootless em 3,66 s. Provas: `probe_test.go` (3, incluindo o runtime quebrado que não pode vencer) + o live |
| GAP-091 | O comando passou a ser `prumo-agent` — **corrigido (descrição)** | W5, `ui.command-surface` | ✅ done | O objetivo — cliente terminal alcançável como `prumo-agent` — está satisfeito, mas a descrição registrava o mecanismo do ADR 015, superseded pelo ADR 016. Não houve renomear de `cmd/prumo` para `cmd/prumo-agent`: esse diretório nunca existiu nesta árvore e o CLI canônico é `prumo` de `cmd/prumo`. O que existe é resolução de binário: `prumo agent` procura `pa`, `prumo-agent` ou `prumo-agent-tui` como irmão do executável e depois no PATH, com `PRUMO_AGENT_BIN`/`PRUMO_TUI_BIN` tendo precedência (`cmd/prumo/tui_commands.go:52-70`). Corrigido porque um registro de auditoria que cita uma decisão superada como prova é pior que um que não prova nada | 0 ✅ |
| GAP-089 | Capacidades de modelo: o TUI não dizia o que cada modelo faz | W5, `ui.component-contracts` | ✅ done | O `models` op devolvia **nomes soltos** e o picker mostrava nomes soltos: um usuário tinha que saber de cor quais modelos enxergam, raciocinam ou aceitam ferramentas. Nenhum provider publica isso de forma legível, então a fonte é uma **declaração do workspace** (`.prumo/models.json`, com schema próprio) — declarado, não inventado: um modelo que ninguém declarou chega como `declared: false` e o picker diz "not declared", nunca "não enxerga". O `models` op ganhou `model_info` (aditivo: `models` continua trazendo ids para quem só escolhe um), o SDK ganhou `ModelInfo`, e o picker mostra `[text reasoning vision tools audio]` com legenda. Isto é a metade que faltava para o envio de imagem: o gate é `vision`, e agora ele existe. Provas: `capabilities_test.go` (4), `models_test.go` (3) |
| GAP-087 | Métricas de gasto: input, cache e output separados | ADR 009/§24.3 | ✅ done | O harness somava input+output em `tokens` e **não distinguia cache**: nenhum adapter lia os campos de cache (`cache_read_input_tokens`/`cache_creation_input_tokens` no Anthropic, `prompt_tokens_details.cached_tokens` no OpenAI-compat) e o evento `usage` publicava só o total. Um token cacheado e um token de entrada têm preços diferentes, então um total esconde o motivo do gasto. Agora os adapters leem e **separam** o cache (no OpenAI-compat ele é subtraído do prompt, que já o incluía), o evento carrega os quatro números, a sessão soma cada um e o rodapé mostra `in · cache · out · $ · ~$/req` — com degradação para só o custo quando a largura não cabe. **As métricas são analisáveis depois**: os eventos de `usage` ficam na timeline (o registro do run) e a exportação da paleta escreve a timeline em texto puro, incluindo os quatro números. Provas: `status_test.go` (5), `TestTheTimelineExportsAsPlainText` (com cache), frames |
| GAP-085 | Canal push: o daemon avisa em vez de o cliente perguntar | ADR 013 §Consequences, HA4, ADR 014 | ✅ done | Protocolo 0.4.0 introduziu op `subscribe` (`{"op": "subscribe", "run_id": "...", "from": cursor}`). O daemon mantém a conexão aberta, envia ack inicial `{"op": "subscribed", ...}` e streama eventos JSONL à medida que ocorrem com latência 0 ms, fechando no término do run (`run.finished` / estados terminais). O SDK ganhou `Client.Subscribe` e o runner do TUI utiliza push por padrão, com fallback gracioso para o ciclo de polling (`PollInterval`) caso o daemon não suporte a operação ou a conexão caia. Provas: `TestDaemonSubscribe` (daemon), `TestRunnerSubscribePush` (runtime/runner) |
| GAP-084 | Diff de arquivo sob demanda no protocolo | ADR 013, ADR 014, `ui.screen-inventory` | ✅ done | Protocolo 0.4.0 introduziu op `diff` (`{"op": "diff", "run_id": "...", "path": "..."}`). O daemon grava diffs das ferramentas em `diffs-<runID>.json` (sem sobrecarregar a timeline com patches a cada reconexão); SDK expõe `Client.Diff`; o TUI permite inspecionar o diff de qualquer arquivo modificado pelo run no diálogo de arquivos (`Enter`/`d` abre viewport scrollável com syntax diff, `Esc`/`q` retorna à lista). Erro explícito se o arquivo não foi alterado. Provas: `TestDaemonDiff`, `TestRunnerDiff`, `TestFilesDialogListAndDiff`, `TestFilesDialogDiffError` |
| GAP-083 | O gate não mostrava o que pedia para aprovar | GAP-046, W11 | ✅ done | O diálogo de aprovação sabe renderizar preview (comando, diff, URL) desde sempre e **nada fornecia os argumentos**: `permission_wait` publicava só `tool` e `request_id`, então aprovar era um salto de fé — e o frame mostrava um diálogo com o rótulo "Command" e nada embaixo. Agora o evento carrega `arguments` (a única exceção à regra de payload mínimo, com a razão escrita no código: um pedido que não carrega evidência não pode ser respondido, só obedecido), o cliente os passa como `Params`, e o header do diálogo deixou de gastar linhas com rótulos vazios — sem isso a evidência não cabia na altura do diálogo. Provas: `TestAGateCarriesWhatItIsAskingAbout`, frame `permission-requested` mostrando `go test ./...` |
| GAP-081 | Documentos canônicos descrevendo um cliente que mudou | W15/authority | ✅ done | Depois de ~80 mudanças, três documentos afirmavam coisas que já não eram verdade. O README do cliente dizia que o mapa de interface e o painel de arquivos eram **devidos** (ambos feitos) e não mencionava a ponte, o modo headless nem a persistência de tema; `docs/architecture/dependency-rules.md` colocava a stack do terminal "apenas em `tui/`" com o renderer em `tui/styles.go` — o cliente arquivado — quando o ADR 013 a levou para `prumo-agent tui`; e quatro `not_applicable_reason` ainda citavam `tui-spike-h10.md` para justificar decisões que hoje são do cliente. Todos reescritos para o que existe. O README do cliente ganhou ainda uma tabela dizendo **onde** cada pergunta sobre o cliente é respondida |
| GAP-080 | O transcript inteiro era renderizado para mostrar uma janela | ADR 009, §24.3 | ✅ done | O render era O(conversa): abrir 400 mensagens custava ~0,5 s e 2000 custavam segundos, **antes da primeira linha aparecer**, e o mesmo a cada mudança de largura. Agora o que se renderiza é a **cauda** — onde o leitor começa e onde estão as palavras mais novas — com um orçamento de três telas, e o resto é renderizado quando a viewport chega ao topo (`renderMore`, que empurra o offset pelo que acrescentou para o leitor não perder a linha). Medido: primeiro paint de 400 mensagens **488 ms → 45 ms**; `BenchmarkResizeStorm` **3,1 s → 172 ms** por passo. O que sobra é o custo de uma tela de conversa, que é o que se vê. Provas: `TestALongTranscriptIsRenderedFromItsEnd`, `TestScrollingToTheTopRendersWhatIsAbove`, e os 51 golden frames inalterados |
| GAP-079 | Resize: um render por passo do arrasto | ADR 009 | ✅ done | O custo por render já tinha caído (GAP-071), mas um arrasto **multiplica renders**: cada tamanho invalidava todas as linhas. Agora uma mudança de largura espera 80 ms — o layout muda na hora, o conteúdo reassenta quando o terminal para — e um settle ultrapassado é descartado em vez de renderizar para uma largura que já passou. Uma altura que muda continua sem invalidar nada. Provas: `resize_test.go` (tempestade de 3 tamanhos → 1 render; altura sozinha não agenda nada) |
| GAP-060 | Evidência visual e fechamento das obrigações de acessibilidade | W9/W11, `visual-constitution` §Visual evidence set | 🟡 partial | Produzido: **51 golden frames** (`prumo-agent tui/internal/tui/testdata/golden/`, 17 estados × 3 classes de largura), capturados como frame buffer em texto puro — com digest no cabeçalho, comparação byte-a-byte e regeneração por `-update-golden` — e **derivados da matriz canônica**, de modo que um estado que passe a ser aplicável sem frame falha (`TestEveryApplicableStateIsCapturedOrExcused`), com as exceções declarando o motivo. Junto vieram o fallback ASCII de glifos (`styles.ResolveIcons`), a matriz de largura sem overflow (`TestEveryWidthClassStaysInsideItsTerminal`), a tempestade de resize, a leitura sem cor, e o indicador de foco (GAP-062). **Duas obrigações foram fechadas** com lastro real: `accessibility.keyboard` e `accessibility.focus` deixaram de ser `not-applicable` e passaram a ter `sources`, `requirement_map` e 3 claims cada, com evidência de documento canônico **e de teste** (as provas estão nos testes que falham se a propriedade sumir, não numa frase). Os motivos das 8 que continuam `not-applicable` foram corrigidos para dizer o que já existe e o que falta — em nenhum caso sobrou uma razão obsoleta, que era o defeito original. Falta o que exige gente: estratégia de anúncio para leitor de tela, política de motion, contraste medido no output renderizado, e a política de copy de erro | estratégia de anúncio + política de motion + contraste medido + copy de erro |

## 1.1 Auditoria de implementação — 2026-09-23

Auditoria estática de 6 subsistemas (runtime/daemon, tool-calling, multiagente,
router, conectores/instalação, protocolo/skills) mais pesquisa comparativa com
opencode, Claude Code, LiteLLM, OpenRouter, LLM Gateway, multica e Agent
Orchestrator. Achados que rebaixam gaps já marcados `✅ done` estão na tabela
acima; os novos estão aqui.

**Regra adotada a partir desta auditoria:** `✅ done` exige **caller de
produção**, não apenas biblioteca implementada + teste verde. A prova é uma
busca por import não-teste. Um componente com só testes é `🟡 partial`.

| ID | Item | Sev | Evidência (arquivo:linha) | Onda |
|----|------|-----|--------------------------|------|
| GAP-097 | Comando que falha reporta exit 0 — **corrigido** | CRITICAL | `aci.go` deriva o status de `*exec.ExitError` (127 binário ausente, 124 deadline, 130 cancelamento) via `runCommand`/`runShell`; `test.run` deixou de usar `\| head -c 8000`, que reportava o status do `head` em vez do do teste. 13 testes de guarda, 4 verificados falhando contra o comportamento antigo. Commit `01bc102` | 0 ✅ |
| GAP-098 | Daemon cria tracker sem teto — **corrigido** | CRITICAL | `NewTracker(0,0,0)` significa ilimitado no envelope, e era o que o daemon construía para toda run: sem teto de tokens, custo ou tool calls até o provider recusar. `Deps.Budget` carrega o teto por run, e valor zero ou negativo cai em `DefaultRunBudget` em vez de significar "sem limite" — que é exatamente a forma do bug antigo. Teto conferido só depois do uso não limita nada, porque a chamada já foi paga: o runtime agora reserva antes da chamada, dimensionada pelo prompt mais uma allowance de turno, e liquida contra o uso real quando o stream reporta. Preflight para uma run sem folga, e `Reserve` recusa uma chamada que não cabe ao lado do que já está reservado, para que chamadas concorrentes não passem individualmente e a run termine acima. Um settlement é registrado mesmo passando do teto: o `Consume` do envelope recusa cruzar um hard limit, o que é certo para um request e errado para uma conta. `ConsumeBudget` foi removido em vez de deixado ao lado, porque dois caminhos contabilizando a mesma chamada fariam double-count. Commit `fe2c62a` | 2 ✅ |
| GAP-099 | Evidência fora do schema canônico — **corrigido** | HIGH | `evidence.schema.json` ganhou `harness_run`; `Validate`/`ValidateRecord` exigem os 6 campos obrigatórios, checam o tipo contra o enum e exigem RFC3339; `WriteEvidence` emite produtor, timestamp, status derivado da execução, goal e confiança `low`; os dois chamadores passam o goal e não descartam o erro. Commit `cf1fba3` | 0 ✅ |
| GAP-100 | Budget só é conferido depois da chamada — **corrigido** | HIGH | A reserva antes da chamada e o preflight que a acompanha são o que resolve isto, e foram entregues com o GAP-098: `runtime.reserveForCall` consulta `BudgetExhausted` e reserva uma allowance dimensionada pelo prompt **antes** de `Models.Stream`, liquidando contra o uso real quando o stream reporta. Conferir só no usage que chega depois não limita nada, porque a chamada já foi paga. A entrada ainda dizia Wave 4 aberta porque o GAP-098 não atualizou esta linha; a evidência nunca esteve ausente, a bookkeeping sim. Commit `98bdd30` (GAP-098) | 0 ✅ |
| GAP-101 | Sem delegação em produção — **parcial** | CRITICAL | `harness/team` continua sem nenhum importador não-teste, e `agent.delegate`/`agent.ask` continuam fora do catálogo ACI e do protocolo. O que o GAP exigia como infraestrutura já existe: `Runner`, `RunWork`, `AllocateWorktree`, `MergeWorkspaces`, `ChildHandoff`. O que falta é a feature do ADR 017 — a tool `delegate` no pai, a fábrica de filho sem referência de volta, o isolamento de contexto, e o caller de produção. **Isto é implementar um ADR aceito, não corrigir um defeito**, e é por isso que fica parcial em vez de fechado: fechar aqui exigiria declarar o trabalho feito e o gateway do wiring como concluídos, que é o que o GAP-011 já acusava. Os invariantes que o ADR torna exigíveis e que eram código morto de segurança foram fechados: GAP-168, GAP-167 e GAP-109 | 2 🟡 |
| GAP-102 | Gateway não está no caminho do runner — **corrigido** | CRITICAL | O gateway tinha seleção, fallback, retry, circuit breaker e filtro de quota — todos testados, nenhum executado, porque o runner recebia o adapter cru. `Gateway` agora implementa `model.Provider`, e o CLI envolve o provider com `routeThroughGateway` nos dois call sites de produção. O call site do runner não mudou: quem quer roteamento passa um gateway, quem quer o adapter cru continua passando o adapter. Duas decisões que vieram junto: um `ModelRequest.NoTransparentFallback` explícito, porque trocar de provider no meio da run depois de um efeito observável é repetir o efeito, não repetir a chamada — o provider seguinte não viu os resultados das ferramentas; e `DeclareTarget` separado de `Register`, porque "este processo fala com este provider" e "aqui está o custo, onde os dados vão e se ele serve ao pedido" são perguntas diferentes, e um provider registrado sem target fica **não classificável** em vez de aprovado por omissão. 3 testes no runtime, um deles verificado falhando ao remover o flag | 0 ✅ |
| GAP-103 | Circuit breaker nunca recupera — **corrigido** | HIGH | `CooldownUntil` era escrito e nunca lido, e um circuit `open` não entrava nem em `healthy` nem em `degraded`, então um provider que passava por um minuto ruim ficava excluído de toda rota pelo resto do processo: o breaker abria e não fechava, e a recuperação que ele existe para enable estava inalcançável. O cooldown agora é consultado e um circuit expirado volta como probe `degraded`. `quotaExhausted` lia o mapa de quotas sem lock enquanto `SetQuota` e o caminho de rate-limit escreviam com lock — race real, agora sob lock, com teste de concorrência. Um cooldown ilegível conta como expirado: um circuit que manda esperar até um tempo que ninguém consegue ler não pode segurar um provider fora do roteamento para sempre | 0 ✅ |
| GAP-104 | PID lock não-atômico e SIGTERM errado — **corrigido** | HIGH | `AcquireLock` lia o lock e depois escrevia: dois daemons juntos liam vazio, ambos escreviam, ambos rodavam. Exclusão agora vem de `O_EXCL`, que o kernel faz atomicamente; um teste dispara 24 acquires concorrentes e exige exatamente um vencedor. PID cru não decide se um lock está vivo porque PID é reutilizado: o registro carrega start time do kernel e boot id, e um lock com processo vivo e start time diferente é tomado em vez de obedecido — que é a aparência de um PID reutilizado. Lock de boot anterior é stale por construção. O formato bare-integer anterior continua sendo lido: um lock deixado pelo formato velho é um lock real, e lê-lo como corrompido o tomaria, que é como dois daemons acabam rodando. `Stop` esperava nada: agora espera o processo sair de fato e reporta quando não saiu dentro do grace period; um lock que não conseguiu sinalizar é deixado no lugar, porque removê-lo deixaria um segundo daemon iniciar ao lado de um que roda. A espera precisou de mais uma coisa: `kill(pid, 0)` responde com sucesso para um zumbi, então um processo que saiu e não foi reaped lia como vivo e a espera penduraria em algo que já parou. Liveness agora lê o state do processo e trata Z e X como ausente. Re-acquire no mesmo processo é recusado, não tratado como reentrante: lock que não exclui não é lock. Commit `0b9dd33` | 2 ✅ |
| GAP-105 | SIGTERM não drena runs — **corrigido** | HIGH | Contextos de run derivavam de `context.Background()`, então nada que o daemon fizesse os alcançava: um SIGTERM fechava o listener e o processo saía com as runs em voo, e checkpoint e registro simplesmente sumiam. Runs agora derivam de um contexto do servidor, e o shutdown para de aceitar, deixa o trabalho em voo sozinho por um grace period para uma model call poder voltar e ser registrada, depois cancela o que sobrou e espera desembrulhar. Cancelar não é terminar: uma run cancelada ainda está no passo de persistência quando o cancel retorna, então o daemon sairia com checkpoint meio escrito reportando drain concluído. Run estacionada não era cancelável de jeito nenhum — o observe já tinha retornado quando ela estacionou, então nada restava para notar o cancelamento e limpar a entrada. Uma run presa em chamada longa mantinha o lock do runner durante todo o step, então o drain esperava a run que pretendia drenar e `opStatus` bloqueava um cliente fazendo polling durante uma chamada longa; `TryStateCopy` reporta runner ocupado em vez de esperar. Commit `728bf87` | 2 ✅ |
| GAP-106 | Aprovações não são recarregadas — **corrigido** | HIGH | `runlayer.LoadPermissions` restaura decisões num engine novo e o daemon chama antes de iniciar a run. Trail ausente é normal (run que nunca bateu em gate); linha corrompida é pulada, porque um registro ilegível não pode inutilizar todas as outras aprovações. Decisão restaurada sem fingerprint não é reutilizada: uma trail anterior a ela identifica o request mas não o conteúdo, e honrá-la reabriria exatamente o buraco que o fingerprint fechou. Commit `28631b8` | 1 ✅ |
| GAP-107 | Aprovação não se liga ao conteúdo aprovado — **corrigido** | HIGH | O id de request deriva do id da tool call, que o modelo escolhe, e a decisão era indexada só por ele: uma chamada posterior com id reaproveitado herdava a aprovação de uma anterior. `perm.Fingerprint` hasheia action, resource, arguments, scope de filesystem, destinos de rede, credential scopes, data class e reversibilidade; uma decisão registrada só é consultada quando o fingerprint bate. O aprovador precisa citar o fingerprint que lhe foi mostrado, e o runner o recalcula da chamada realmente pendente antes de comparar. `permissionRequestFor` é o ponto único de construção do request, e `summarizeArguments` ordena as chaves para o fingerprint não depender da ordem de iteração do map. Commit `8fb4622` | 1 ✅ |
| GAP-108 | Backoff linear e sem `Retry-After` — **corrigido** | HIGH | O adapter reduzia a resposta a uma string, então o gateway adivinhava o motivo procurando `429`, `quota`, `overload` na mensagem — e `RESOURCE_EXHAUSTED`, o sinal da Google, não estava na lista, então um provider esgotado era tratado como falha comum e retentado direto no limite. O header `Retry-After` era descartado. Agora a resposta é parseada uma vez no adapter, onde status e headers ainda existem: `model.ProviderError` carrega a classificação e o intervalo pedido, e `ModelEvent` transporta `Quota` e `RetryAfter` até o gateway (o erro tipado morria na fronteira do evento se parasse no adapter). O backoff é exponencial com teto, e o `Retry-After` do provider **sobrescreve** o cálculo — adivinhar menos que o intervalo pedido é reentrar na janela do limite. Substring continua como fallback para quem não passa por adapter, com a lista completa | 0 ✅ |
| GAP-109 | Limite de delegação não existe em código — **parcial** | HIGH | "Um nível, fixo" (decisão 1 do ADR 017) hoje é verdade por **ausência**: `agent.delegate` não existe no catálogo, então nenhum runner — pai ou filho — pode delegar. Uma decisão estrutural apoiada numa ferramenta que ninguém adicionou é frágil no sentido exato que o ADR cita: basta alguém acrescentar a tool e o teto de um nível desaparece sem que nenhuma configuração tenha mudado. O que fecha isso é a decisão 3, construir a tool de delegação com uma fábrica de filho que não tem referência de volta, e é o mesmo trabalho restante de GAP-101 | 3 🟡 |
| GAP-110 | Symbolic links escapam do workspace — **corrigido** | CRITICAL | `internal/harness/safepath` passou a ser a fronteira única. `Resolve` testa contenção lexical e de novo após `EvalSymlinks`, resolvendo o ancestral existente mais profundo para pegar arquivo novo atrás de parent symlinkado. As 4 reimplementações lexicais (`aci`, `toolgateway`, `environment`, `directive`) foram migradas. Verificado contra a implementação antiga: ela aceitava `escape/secret.txt` e lia o arquivo. Commit `11bbc4c` | 1 ✅ |
| GAP-111 | Base URL de provider sem allowlist (SSRF) — **corrigido** | CRITICAL | `model.DestinationPolicy` + dialer próprio: a checagem no dialer, não só na construção, porque resolver o nome e conectar depois abre janela para DNS responder com um endereço na checagem e outro na conexão. Recusa por padrão loopback, RFC1918, link-local, unique-local, endpoints de metadata e http simples. `localhost` e hostnames de metadata são recusados já na validação. `ForName` e a factory padrão do daemon validam, então a proteção vale sem cada chamador opting in. Commit `0cadfc0` | 1 ✅ |
| GAP-112 | Identificador usado como path sem validação — **corrigido** | CRITICAL | `safepath.ValidateID` é a checagem que não existia. `run_id` vinha da requisição e ia direto para o nome do arquivo em record, event, context manifest e checkpoint stores; um valor com separador escrevia fora do store. `adoption` validou os `TargetPath` do proposal com `Resolve`. `daemon` rejeita o id na fronteira da requisição em vez de começar uma run que nada registra. Commit `11bbc4c` | 1 ✅ |
| GAP-113 | Imagem lida fora do workspace — **corrigido** | HIGH | A referência era unida ao workspace com `filepath.Join` e o que saísse era lido: `@../../etc/passwd` é uma referência de imagem legal pelas regras antigas, e caminhos absolutos eram aceitos outright. O request ao modelo é montado a partir da conversa, então um caminho apontado para fora do workspace era lido e embutido no pedido. A run compilava um contexto, escrevia em disco e não entregava nada ao modelo — e agora também lê um arquivo que não é dela. Passa por `safepath.Resolve`, que resolve o symlink e rejeita quem sai, e ganhou teto agregado (32MB): o teto por imagem existia, o teto por request não, e oito imagens abaixo do teto por imagem são oito vezes o orçamento de qualquer um. 3 testes | 0 ✅ |
| GAP-114 | Tools nativas não recebem schema; Anthropic não envia tools — **corrigido** | CRITICAL→HIGH | Anthropic declarava `ToolCalls: true` e não colocava schema nenhum no request, então um modelo real não tinha como saber que as tools existiam. Três coisas precisavam ser verdade e nenhuma era: as tools nativas não tinham schema (o catálogo as descrevia em prosa e os handlers liam argumentos que nada declarava); ninguém perguntava ao executor o que ele podia rodar (o runtime deriva o catálogo de `Svc.Tools`, e `CountingTools` encaminha, porque o executor do daemon é o wrapper); e o Anthropic mandava a lista no formato errado, que lá é ignorado. Um tool sem schema recebe um objeto vazio explícito em vez de nada — um modelo pedidos para chamar uma tool sem argumentos declarados não manda nenhum. `requiredProperties` lia só a forma decodificada de JSON, então os argumentos obrigatórios de todo schema escrito em Go — toda tool nativa — eram silenciosamente não aplicados. Commit `98bdd30` | 3 ✅ |
| GAP-115 | Resultado de tool não é feedback válido de provider — **corrigido** | HIGH | O resultado chegava como mensagem solta, sem call à qual se ligar, e o turno do assistant que pediu a tool **nunca era registrado**. Uma conversa que relata o resultado de uma chamada que ninguém fez não é uma que qualquer provider aceite. O runtime agora registra os dois. Cada serializer fala o seu dialeto: OpenAI recebe `tool_calls` no turno do assistant e `tool_call_id` no resultado; Anthropic recebe blocos `tool_use`/`tool_result`, com o resultado como o turno `user` que esta API exige — antes achatado em prosa. Dois achados dos testes: o papel interno `agent` ia cru e esta API só aceita `system`/`user`/`assistant`/`tool`, então toda tool round trip era enviada com um papel que o provider recusaria; e o render da falha tinha sido feito só para o Anthropic, então a tool que falhava continuava chegando ao caminho OpenAI como o resultado vazio de sempre. Commit `0244d25` | 3 ✅ |
| GAP-116 | `ToolResult.Error`/`ExitCode` descartados — **corrigido** | HIGH | Uma tool que falhava punha o motivo em `Error` e deixava `Output` vazio, e só `Output` era registrado. O modelo recebia um resultado vazio de uma chamada que tinha falhado, o que se lê como chamada que deu certo e não retornou nada — passava a raciocinar de uma premissa falsa. `Observation` agora carrega o veredito da própria tool, o motivo, o exit code e se a saída foi truncada. O veredito é método e não expressão em cada call site, porque "isto funcionou" tem duas respostas e quem checa uma só reporta tool que falhou como sucesso, ou o inverso. Tool pode legitimamente não retornar nada, e é por isso que o veredito é da tool e não inferido de saída vazia. Falha sem mensagem ainda ganha um motivo com o exit code: silêncio é o pior caso. Commit `7c0003f` | 3 ✅ |
| GAP-117 | TurnID/requestID não avançam entre turnos de tool — **corrigido** | HIGH | `TurnID` era setado uma vez quando o runner era construído e nunca movia, então todo request de model da run carregava o mesmo turno e o mesmo request id. Como a `IdempotencyKey` de uma tool call é derivada do request id, dois turnos diferentes chamando a mesma tool colidiam nela, e toda observation da run carregava o mesmo turno — nada dizia qual turno produziu o quê. O turno avança em `EvaluateStop`, junto com `TurnsDone`, e o número vem de `TurnsDone` em vez de fazer parse do id atual: uma run retomada com cinco turnos continua em turn-6 em vez de reusar um identificador que a vida anterior já gastou. 3 testes, o principal verificado falhando com o id repetido nas três chamadas | 0 ✅ |
| GAP-118 | IDs de evento colidem — **corrigido** | HIGH | IDs eram derivados de `len(payload)`, então todo evento do mesmo formato recebia o mesmo número: todo `text_delta` era `ev-2`, todo `usage` `ev-6`. O dedup do subscribe era `if sentIDs[id] { continue }`, então um evento ao vivo que dividisse id com um replay era descartado como repetição e o cliente nunca o via. Isso é evento perdido, não duplicado. IDs vêm de um contador por run no state, então viaja no checkpoint: uma run retomada continua a sequência em vez de reemitir ids já usados. O dedup agora compara o evento inteiro, então id compartilhado com conteúdo diferente é colisão e é enviado; evento sem id nunca é deduplicado. Commit `843e0cd` | 2 ✅ |
| GAP-119 | Assinantes lentos descartados; cursor quebra na rotação — **corrigido** | HIGH | Duas metades. **O assinante**: o fan-out mandava com `select`/`default` e o evento era descartado em silêncio quando a fila estava cheia. Uma run esperando aprovação cuja pergunta foi descartada espera para sempre, e o cliente não tem como distinguir uma run quieta de uma que perdeu eventos. Agora o assinante é **desconectado** e a queda é contada: o log é o registro do que aconteceu, o push é conveniência, e uma conveniência que falha em silêncio é pior do que nenhuma. O canal é fechado para o leitor bloqueado acordar. **O cursor**: `from` era um índice posicional, e a rotação guarda a segunda metade — então o índice 40 nomeia um evento diferente do que nomeava um instante antes. Um cliente que reconecta perdia silenciosamente tudo entre onde parou e onde a rotação cortou, e relia o que já tinha visto; com o log encolhido além da posição, a reconexão virava erro duro. O cursor agora é o **id do evento**, que sobrevive à rotação porque os eventos que ele nomeia são os que foram mantidos. Um id que não está mais lá é dito, e o cliente relê o log retido em vez de retomar de uma posição que não aponta mais para ele. **A primeira versão da correção tinha uma corrida**: copiava a lista de canais e mandava depois, o que deixava um assinante recém-desconectado ter o canal fechado no meio do envio — panic, não evento perdido. O fan-out inteiro passou a rodar sob o lock, que é o que torna envio e fechamento mutuamente exclusivos. 7 testes | 0 ✅ |
| GAP-120 | Erro de persistência vira sucesso — **corrigido** | HIGH | Três caminhos descartavam todo erro com `_`. `saveJobs` fazia `Marshal`/`MkdirAll`/`WriteFile`/`Rename` sem olhar o resultado: disco cheio ou diretório sem permissão fazia schedule, unschedule e o bookkeeping do ticker responderem sucesso com a mudança perdida. `saveRecord` deixava uma run executar sem registro nenhum — sem status, sem fase, sem stop reason — e uma run que ninguém consegue consultar é indistinguível de uma que nunca aconteceu; agora a recusa de partida, e uma falha ao gravar o desfecho marca a run como `failed` em vez de `complete`, porque uma run cujo resultado não está gravado não pode ser distinguida de uma ainda em voo. `appendEvent` deixava a trilha de auditoria de uma run parar no meio sem dizer nada — e um log de auditoria com um buraco silencioso é pior do que nenhum log, porque parece completo; a falha é devolvida e contada em `EventWriteFailures`, que é o que torna o buraco encontrável. Um `max_retries` que `opSchedule` aceitava e ignorava apareceu no caminho e foi corrigido | 0 ✅ |
| GAP-121 | Scheduler tem lost-update — **corrigido** | HIGH | `jobs.json` guardava a schedule inteira e cada operação lia, editava a sua cópia e gravava de volta, sem mutex: o ticker roda em goroutine própria enquanto schedule e unschedule chegam em handlers de conexão, então o escritor mais lento apagava o mais rápido e um job agendado sumia porque outra coisa salvou no instante seguinte. O lock é do ciclo read-modify-write inteiro, não das operações de arquivo — segurá-lo só na escrita ainda deixa dois leitores construindo cópias conflitantes. `tickJobs` passou a **reservar** o job sob o lock e persistir `NextRun` antes de despachar: um tick concorrente vê que o job não está mais due e não dispara de novo, que é o mesmo defeito de um cron que só marca "started" quando o trabalho termina. O dispatch fica fora do lock porque iniciar run é a parte lenta e segurar o lock nela faria todo schedule esperar. E o `max_retries` era aceito e ignorado por `opSchedule`, apesar de documentado em `Job` — corrigido no caminho | 0 ✅ |
| GAP-122 | Recursos do daemon sem limite — **corrigido** | HIGH | Seis recursos que um caller controla não tinham teto nenhum, e nenhum deles exige malícia: um handler que não retorna segura uma goroutine para sempre; um cliente que abre milhares de conexões segura descritores; uma run sem deadline segura a conexão com o model pelo tempo que o provider quiser; um assinante que não lê segura um buffer; e uma run esperando uma aprovação que ninguém vai dar segura workspace, orçamento e um slot — um aprovador que fecha o notebook basta. `Limits` com `withDefaults` cobre os seis, e `withDefaults` existe porque um caller que configura um teto trocava um limite visível por todos os outros invisíveis. **Um achado no caminho**: a primeira versão criava o contexto com deadline da conexão e nada o observava enquanto o handler estava em `Scan` — o limite estava declarado e não fazia nada, que é exatamente o padrão que esta auditoria existe para achar. O deadline vai no socket e é renovado por leitura, o que limita *esperar por um request* e não a vida de uma conexão: um assinante que continua falando não é cortado. 7 testes, um deles abrindo uma conexão real e verificando que o daemon a fecha | 0 ✅ |
| GAP-123 | Checkpoint incompleto; `resume` marca Complete — **corrigido** | CRITICAL | `Checkpoint` carrega `Messages`, `ToolQ`, `Obs`, `AfterSideEffects` e `TurnsDone`; `persist` grava e `Runner.RestoreFrom` reconstrói copiando. `agent resume` recusa checkpoint sem conversa em vez de declarar conclusão, e quando continua usa o provider do chamador e `RunUntilDone`. Recuperação de runs ativos do daemon segue em GAP-164. Commit `523790f` | 0 ✅ |
| GAP-124 | Goal locks e aprovação — **corrigido** | HIGH | `TransitionGoalWithEvidence` resolve cada entrada contra registros reais: válido, não-stale e pertencente ao goal; registro nulo falha fechado. `cliops.GoalState` lê o evidence store antes da transição. 10 testes, 6 verificados falhando contra `len(evidence)==0`. Commit `76ff712` | 0 ✅ |
| GAP-125 | Workspace do run aplicado na camada errada — **corrigido** | HIGH | `SetWorkspace` recebia o workspace **do daemon**, e o workspace do request só era lido depois: uma run que nomeasse outra árvore recebia tools enraizadas nela e um provider enraizado em outro lugar — e um provider que delega edita o que lhe foi dado. A resolução do workspace sobe para antes da construção do provider e é a mesma que vai para a run | 0 ✅ |
| GAP-126 | Journal de side-effect fora do caminho real — **corrigido** | CRITICAL | Os 4 runners de produção (CLI, resume, team binder, daemon) agora gravam intent antes da chamada e outcome depois. `RecordIntent` decidia `return true` e executava em todo resume; agora a recovery policy decide: retry segue, skip não, fail e política ausente param a run com erro. Read-only/idempotent gravam retry; side-effecting/destructive gravam skip. A política gravada com o efeito governa o replay, então um skip deliberado não é sobrescrito pelo kind atual. Erro de transporte grava failed em vez de deixar pending, porque a chamada não tomou. Commit `75f9260` | 1 ✅ |
| GAP-127 | Firewall de diretiva só em teste — **corrigido** | HIGH | O daemon construía toda run com `NewRunner`, então o firewall que protege as tool calls era exercitado por um teste e por nada mais. Uma run de produção não tinha escopo: o modelo alcançava tudo que as tools alcançavam, e o único limite era o permission engine — que pergunta a um humano, e uma run de daemon não tem humano assistindo. **Quatro defeitos que só apareceram ao ligar**: `NewRunnerWithDirective` tirava o **run id do task id** da diretiva, então todo evento saía com um id que o caller não estava acompanhando; o firewall só aceitava caminho **relativo**, e as tools que ele protege são chamadas com o workspace resolvido — um firewall que recusa os caminhos que as próprias tools produzem é um firewall atrás do qual ninguém roda; os boundaries de mutação eram gravados em coordenadas absolutas enquanto a comparação é relativa; e o `withDefaults` não cobria o novo campo de budget, que é a armadilha que o GAP-122 já tinha documentado. Uma run sem workspace ou sem teto de custo **não começa**. 5 testes, um deles ponta a ponta pelo `dispatch` real do daemon | 0 ✅ |
| GAP-128 | Manifesto de contexto não chega ao modelo — **corrigido** | HIGH | A run compilava um contexto, gravava em disco e guardava o id no state — e depois ninguém lia nada dos dois. O request ao modelo é montado a partir da conversa, então os arquivos que a run julgou relevantes, o nível de divulgação e a pressão nunca chegaram ao modelo que deveria agir sobre eles. Compilar um contexto e não entregá-lo é fazer o trabalho e guardar a resposta. O hook passou a devolver **o texto** que o modelo recebe, e o runtime o coloca na conversa como mensagem de sistema uma vez — presente em todo request, porque é parte da conversa, e sem acumular uma cópia a cada turno. **Dois erros meus, ambos pegos por teste**: a primeira versão entregava também o id placeholder quando não havia hook, botando o texto literal "ctx-manifest" como contexto e deslocando toda a conversa — o que quebrou um teste de visão que estava certo; e meu próprio teste afirmava que o contexto não podia aparecer em todo request, o que é o oposto do desejado. 3 testes | 0 ✅ |
| GAP-129 | Custo ausente em OpenAI/Anthropic — **corrigido** | CRITICAL | Os dois endpoints devolvem tokens e nenhum custo, então todo evento de usage carregava `CostUSD` zero e `--budget-usd` comparava um número fixo contra um teto que o gasto nunca alcançava. `modelregistry.PricingTable` versionada, effective-date e fonte por entrada, com custo derivado no ponto onde a usage é mapeada. Três coisas apareceram ao ligar isso: o `ProviderKey` default era o nome do adaptador (`openai-compat`), que não existe na tabela, então **nada** era precificado; o Anthropic anuncia entrada em `message_start` e saída em `message_delta`, então o último evento era só a saída e toda run subdeclara o que pagou na entrada; e um modelo sem preço agora é distinguível de um modelo gratuito por `modelregistry.Pricable`, em vez de reportar zero silencioso. 9 testes, incluindo a semântica de uso dividida e o cache contado uma vez só. Commit `605c2ab` | 1 ✅ |
| GAP-130 | Cache/reasoning fora do total — **corrigido** | HIGH | O medidor de tokens contava só entrada e saída, em dois lugares que precisam concordar: `runlayer.ConsumeUsage` e o settle da reserva no runtime. Tokens de cache são servidos pelo provider, então uma run com o prompt inteiro em cache reportava total perto de zero e atravessava um teto que tinha de fato preenchido — a mesma brecha que o medidor de custo teria se contasse uma dimensão só. `Usage.TotalTokens()` é agora a definição única, e o reserva/settle do runtime usa ela. `ReasoningTokens` foi acrescentado e **não** entra em total nenhum: o provider já o inclui no completion, somar seria cobrar o mesmo token duas vezes; ele existe para distinguir uma run que pensou de uma que falou. 4 testes, incluindo a prova de que 400 tokens de reasoning não mudam o total nem o custo. Commit `bc6dffc` | 1 ✅ |
| GAP-131 | Stream truncado vira `completed` — **corrigido** | HIGH | `scanner.Err()` não era checado, e o fim do laço emitia `completed` como se o loop tivesse terminado com sucesso. Os dois endpoints sinalizam conclusão explicitamente (`[DONE]` e `message_stop`); a conexão cair, a leitura falhar ou uma linha estourar o buffer não é o provedor dizendo que acabou, e uma resposta truncada ficava registrada como run concluída sem nada indicar que a cauda sumiu. Ligar isso revelou um furo maior embaixo: o runtime só falhava em erro **não-retryable**, sob o pressuposto de que algo acima tentaria de novo — mas o gateway não está nesse caminho (GAP-102), então um 429, um 500 ou um corte de stream eram engolidos e o laço avançava como se o modelo tivesse terminado, com a resposta parcial na conversa e a run salva como sucesso. Qualquer erro agora encerra o turno, e o flag continua no erro (`runtime.Retryable`) para a camada que faz retry distinguir as duas origens. 5 testes, todos verificados falhando contra o código anterior. Commit `ca1223b` | 1 ✅ |
| GAP-132 | Política de role declarativa e inconsistente — **corrigido** | HIGH | Duas metades. **A inconsistência**: o writer emitia cada role como `{preferred, fallback}`, o doctor esperava o nome de um profile e fazia `continue` em qualquer outra coisa — que é toda role que o writer produz, então a checagem nunca examinou um arquivo gerado pelo framework e passou por pular o trabalho. Ao ligar as pontas apareceram duas falhas mais embaixo: nada verificava que os ids de modelo de uma role existem no roster, e o `additionalProperties` com valor-esquemático era lido como bool e pulado (subfalso de GAP-151), de modo que o schema declarava uma constraint que não restringia nada. O shape é definido uma vez, com as duas pontas passando por ele, validado na geração sobre o JSON realmente gravado. **O roteamento**: `DirectiveIR.ModelRoute` não tinha consumidor, e `NewRunnerWithDirective` só tinha caller de teste (GAP-127). Fechar o gateway (GAP-102) deu ao roteamento o caminho que faltava; o que resta é a diretiva chegar ao provider, que é GAP-127. 12 testes | 0 ✅ |
| GAP-133 | Nenhum catálogo de model/provider/pricing — **corrigido** | HIGH | O registry nomeava providers de sua própria invenção — `local`, `trusted-cloud`, `external-cloud` — que não existem em factory nenhuma, então um descriptor podia descrever algo não-construível e nada notava até uma run tentar rotear. O catálogo vira a folha que tudo importa: `ProviderSpec` mora em `modelregistry` (não em `model`, que já o importa — a primeira tentativa criou ciclo), a factory lê essa lista, e `Register` passou a devolver erro recusando provider desconhecido, listando o que existe. Distinção que o caso expôs: "o Prumo constrói" e "isto é um provider real" são perguntas diferentes, e o teste existente registava `ollama` e `openai` — modelos servidos pelo deployment, não por esta fábrica. `External` marca esses; o conjunto é nomeado, senão qualquer typo passa por um deles. Os três nomes antigos agora são recusados, verificado | 0 ✅ |
| GAP-134 | Execução de recipe não é DAG — **corrigido** | CRITICAL | Duas falhas, e só a primeira aparece na forma do código. `step.Next[0]` descartava todos os outros ramos: uma recipe que declara fan-out executava o primeiro, ignorava o resto e retornava sucesso — metade do trabalho descartado, reportada como inteiro. E um step cuja capability não tivesse handler registrado era substituído por uma função que retorna `nil`: um typo no nome da capability produzia um step que não faz nada e reporta que funcionou, que é o pior dos dois porque o que falha alto pelo menos é notado. A execução agora é uma caminhada topológica: um step roda quando todos os steps que apontam para ele terminaram, que é o que faz um join — dois ramos convergindo — funcionar. `FailurePolicy`, declarado e nunca lido, agora significa algo: `continue` marca o step como feito, o que desbloqueia só os ramos que não dependem dele. A ausência de política significa parar, porque seguir em frente diante de uma falha não declarada é uma decisão que alguém precisa tomar de propósito. Passos prontos saem em ordem de id, então a execução é reprodutível — e nenhum handler é chamado concorrentemente, o que importa porque um handler é código arbitrário de quem chama e este tipo não promete nada sobre thread safety. Rodar ramos independentes em paralelo é uma melhoria real e **não** foi feita aqui: exigiria um contrato de concorrência para todo handler existente. Dois bugs meus no caminho, ambos pegos por teste: recalcular o conjunto de prontos do zero devolvia passos ainda enfileirados e os executava quatro vezes, e um teste meu registrava capability errada e por acaso confirmava o check de handler ausente em vez de testar compensação. 7 testes novos, 3 verificados falhando contra o código anterior | 0 ✅ |
| GAP-135 | Handoff é artefato, não protocolo | HIGH | `Build`/`Validate` só checam string não-vazia (`handoff/handoff.go:23-61`); sem resolução de alvo, dispatch, ack ou continuação | 5 |
| GAP-136 | IDs padrão de receita e run colidem | MEDIUM | `R-agent-1` (`agent_commands.go:145-148`) e `R-daemon-1` recomeçam a cada boot (`daemon.go:84-88`, `134-141`) | 6 |
| GAP-137 | Manifesto de instalação não é multi-projeto — **corrigido** | HIGH | Havia **um** arquivo de cleanup por connector, sem projeto dentro. Instalar um connector num segundo projeto sobrescrevia o registro do primeiro: o uninstall limpava o último projeto instalado e deixava os arquivos do outro órfãos, sem nada apontando para eles. Cinquenta projetos significavam quarenta e nove conjuntos de arquivos esquecidos — e o `prumo install --uninstall --connectors` varria o manifesto, que também guarda um connector por ID, então só limpava um. O registro agora é indexado por connector **e** raiz de projeto, e o uninstall varre o índice. O projeto entra como digest do caminho absoluto: um nome legível teria que ser escapado e colocaria o layout de diretórios de um usuário dentro de um nome de arquivo. Desinstalar um projeto **não** esquece os outros. Um registro ilegível é reportado como resíduo, não pulado em silêncio — um connector cujas contas não fecham é um connector cujos arquivos continuam lá depois de um uninstall que disse ter removido. 5 testes | 0 ✅ |
| GAP-138 | Versão de release inconsistente — **corrigido** | CRITICAL | Três superfícies divergiam da fonte, e o gate existente não via nenhuma delas — ele comparava só a política de ciclo de vida de documentação com `CLIVersion`. `scripts/install.sh` e `install.ps1` fixavam `v0.5.0` com a árvore em `0.6.0`, então um `curl \| sh` instalava um binário duas versões atrás; e `.github/workflows/release.yml` disparava em `v0.5.*`, então **taggear a versão atual não publicava nada**. Nenhum dos dois aparecia em qualquer gate, e os dois estavam verdes. A fonte da correção é a política canônica, não uma escolha: `docs/governance/release-policy.md:43` diz `v0.6.*`, então era o workflow que tinha derivado, não a política. A correção durável é o gate: `checkReleaseSurfaces` compara instaladores e workflow com a versão construída, porque um gate que só lê `docs/` não pode notar que aquilo que o usuário curl está duas minors atrás. Cada superfície é extraída pelo ponto onde realmente decide a versão, não por busca de string — a primeira versão buscava, e um comentário mencionando a versão certa satisfazia o gate, então o workflow passava enquanto ainda disparava na tag errada, que é exatamente o silêncio que o gate existe para evitar. "Não consegui ler" também é finding: uma superfície cuja versão não dá para ler pode estar entregando qualquer coisa. 8 testes | 0 ✅ |
| GAP-139 | README e CI carregam nomenclatura superada pelo ADR 016 — **corrigido** | CRITICAL | As duas superfícies vivas já estavam certas e a entrada continuava marcada como aberta: `.github/workflows/ci.yml:31` compila `./cmd/prumo` para `/tmp/prumo`, e `README.md:9` descreve o ADR 016 corretamente. O alias `pa` que o README afirma existe de fato, em `cmd/prumo/tui_commands.go:59`. **O que não foi corrigido, deliberadamente**: sobram 5 referências a `cmd/prumo-agent` em `docs/migration/**` e `docs/runtime/d2-progress.md`, e todas são classificadas como `historical` no `docs/AUTHORITY_MAP.json`. Reescrever um registro histórico para parecer atual é a forma de tornar a autoridade documental sem valor — elas descrevem o estado da época e é por isso que existem. O que era proibido — README e CI apontando para um diretório inexistente, o que fazia o job de docs falhar — foi corrigido | 0 ✅ |
| GAP-140 | Binário instalado depende do checkout — **corrigido** | HIGH | Os assets estavam embutidos no binário e **nunca ligados**: `resources.DefaultFS` ficava nil, toda leitura ia para disco, e a raiz do repositório era achada subindo a partir do executável e depois do diretório de trabalho. Quando nenhuma das duas tinha um checkout, caía num diretório home hard-coded da máquina de um desenvolvedor e depois em `"."` — o diretório de onde alguém rodou. Um Prumo instalado lia os schemas que por acaso estivessem ao lado, e uma checagem de versão podia ser satisfeita por um checkout alheio. Os assets agora são a fonte, e o wiring é um `init()` — em `main()` o `run()` chamado de teste nunca os teria. O caminho do home foi removido e o fallback `"."` também: sem checkout, a resposta é que não há checkout. 4 testes, dois deles compilando o binário e rodando-o num diretório que não é repositório | 0 ✅ |
| GAP-141 | Connector pode sobrescrever e depois apagar arquivo do usuário — **corrigido** | CRITICAL | Codex, Claude Code, Gemini e Antigravity escreviam `AGENTS.md`/`CLAUDE.md`/`GEMINI.md` na raiz do projeto sem checar existência, e depois acrescentavam o path à lista de criados — então o uninstall **removia** o arquivo. São convenções que o usuário possui e costuma usar para as instruções do próprio projeto: a perda era silenciosa e sem volta. A correção tem duas partes porque uma sozinha não fecha. **Escrita**: `WriteManaged` distingue três desfechos que o código antigo tratava como um — não existe, escreve e reivindica; existe e é byte a byte igual ou traz o marcador, atualiza e reivindica; existe, difere e não é nosso, **não toca** e diz. Um arquivo recusado não entra na lista de criados e aparece em `PreservedPaths`, porque uma instalação que pulou seu `AGENTS.md` em silêncio é indistinguível de uma que o escreveu. **Remoção**: `RemoveManagedPaths` passou a receber `home` e a recusar o que não consegue provar que é nosso. O que torna a decisão possível é que há dois diretórios de partidos diferentes — sob `home` tudo é estado do framework, e sob o projeto um arquivo é do usuário salvo se tiver o marcador. A lista de diretórios do framework é nomeada, não inferida de "tem um diretório no caminho", senão qualquer diretório do projeto contaria como nosso e a recusa não valeria nada. Três erros meus no caminho: o marcador é um comentário HTML e quebrava todo JSON gerado; a regra byte-a-byte impede o manifesto de posse de registrar a si mesmo, porque a lista muda por definição; e WriteRecord é a exceção documentada para esse arquivo. 12 testes no install, 1 no connector fazendo o caminho inteiro | 0 ✅ |
| GAP-142 | Capabilities de connector declaradas e não implementadas | HIGH | opencode 9→~3, codex 4→2, claudecode 5→2, antigravity 6→2, gemini 4→2; `Codex AgentProvider` declara `usage`/`cancel`/`resume`/`events`/`permissions` e todas são stub (`extagent/extagent.go:372-402`). O testkit só negocia, nunca executa (`connectors/testkit/testkit.go:143-191`) | 7 |
| GAP-143 | MCP sem initialize e não anunciado ao modelo — **corrigido** | HIGH | `tools/list` saía como primeira mensagem do fio. Um servidor conforme o protocolo não tem sessão para responder: a inicialização é a primeira troca, e um `tools/list` antes dela é erro ou lista vazia dependendo de quão permissivo o servidor for — ou seja, o cliente funciona contra o servidor de teste e falha contra o real. Agora há handshake (`initialize` + notificação `initialized`), uma vez por cliente, e a falha é lembrada em vez de re-tentada a cada chamada. A parte do `Fanout.Specs` que sumia com as tools nativas já tinha sido resolvida no GAP-114, e o runtime já caía em `Tools.Specs` quando `ToolSpecs` é nil. **Dois bugs mais antigos que o achado principal expôs**: `PipeTransport.Close` só marcava uma flag, então um leitor bloqueado nunca voltava (vazamento de goroutine) — e notificações e requests dividiam a mesma fila no `HTTPTransport`, de modo que uma notificação podia ser desfilada no lugar do request seguinte, e o caller lia a resposta vazia dela como se fosse sua. 4 testes novos, um deles com um servidor que **recusa** tools/list antes do initialize | 0 ✅ |
| GAP-144 | Secrets expostos a processos filhos — **corrigido** | HIGH | `internal/harness/childenv` monta o env do filho por allowlist, não denylist: uma denylist precisa acertar todo nome sob o qual um segredo chega, e o próximo provider traz um novo. Passam PATH, HOME, locale, terminal, tmp e proxy; o resto precisa ser nomeado. Credenciais do próprio harness são recusadas no pass-through mesmo allowlisted — alcançá-las é uma declaração, não override de filtro, então vai por Extra. MCP, ACP e opencode usam; o stderr do opencode é redigido antes de chegar ao modelo. Commit `220b95a` | 1 ✅ |
| GAP-145 | Adoption fabrica aprovação — **corrigido** | HIGH | Três defeitos. **`--audit-only` era lido e jogado fora** (`_ = auditOnly`): a flag que promete não mudar nada aplicava migrações assim mesmo — o mesmo comando que `--apply`, com um nome tranquilizador. **A aprovação era fabricada**: ator `"operator"` no tempo `"now"`, um papel em vez de uma pessoa e um instante em vez de um momento, então toda migração da história foi aprovada pelo mesmo ninguém e nenhuma hora podia ser conferida. **O snapshot vinha depois da mutação**, que é justamente o que ele deveria preservar: um snapshot tirado depois é uma cópia do resultado, e reverter devolve o arquivo migrado com a versão que provocou a migração destruída. A aprovação agora nomeia a conta real (`os/user`, com fallback para o ambiente) e um timestamp RFC3339, e registra que veio de `--apply`. **Uma decisão que quase tomei errado**: primeiro eu segurei toda proposta com `ReviewRequired`, achando que auto-aprovar era fabricar — mas *toda* proposta gerada tem esse campo, então isso tornaria `--apply` um comando que nunca pode fazer nada. A flag é a autorização; o defeito era o registro, não o ato. 6 testes | 0 ✅ |
| GAP-146 | Resolver não é task-aware | HIGH | ignora `modes`/`activation`/`requires_any`/`conflicts`/`context_budget` (`resolver/resolver.go:191-344`); bundles são selecionados inteiros contra `framework/specs/ch21.md:104-110`; sem razão de rejeição | 9 |
| GAP-147 | Workforcesync baixa pacote incompleto — **corrigido** | HIGH | Um pacote de skill é um **diretório**, e só `manifest.json` e `SKILL.md` eram baixados: todo check, script, reference, template e example era descartado em silêncio. A skill instalava, a versão batia, e a coisa que faz o trabalho não estava lá. Pior, o checksum do lock cobria **só o manifest** — um `SKILL.md` alterado, um script trocado ou uma reference editada deixavam o lock reportando exatamente o mesmo hash. O lock dizia "é a versão que você pediu" sobre um pacote cujo conteúdo tinha mudado por baixo. Agora o sync lê o manifest, pergunta ao servidor a listagem de cada diretório de recurso e baixa a árvore inteira; o checksum cobre caminho **e** bytes de cada arquivo, então um arquivo que se move é uma mudança e o resultado não depende da ordem do filesystem. **Um vetor de escrita que apareceu pelo caminho**: um nome de arquivo vindo de uma listagem remota é juntado ao destino, então um nome com separador de caminho é o remote escolhendo onde a escrita cai — e é rejeitado. E o `defer resp.Body.Close()` estava dentro do loop, segurando N corpos abertos. 7 testes | 0 ✅ |
| GAP-148 | `go test ./...` da raiz não alcança `prumo-tui` | MEDIUM | módulo aninhado (`prumo-tui/go.mod`); GAP-055 documenta e CI cobre, mas a suíte local não | 0 |
| GAP-149 | `contextv2` ciclo de dependência recursiona infinito | MEDIUM | dependências expandidas antes de `inSet` ser atualizado, sem proteção (`contextv2/contextv2.go:177-199`) | 8 |
| GAP-150 | Artefatos gerados não satisfazem o próprio schema — **corrigido** | HIGH | Três geradores escreviam arquivos que o schema do projeto rejeita. **`prumo.json` do adoption**: seis chaves na versão 1, onde o schema exige dez seções e põe mínimo 2 na versão — o framework criava um arquivo que reprovava na própria validação, e todo gate depois disso reportava sobre um manifesto que ele tinha acabado de prometer válido. **`task`**: `id`, `label` e `dependencies`, onde o schema exige oito campos e chama o título de `title` — o DAG que passava em `CheckDAGCycles` não era um grafo de tasks que o framework conseguisse carregar. **Decisão deliberada**: `ai.preferred_models` recebe um sentinela visível, não um roster. O adoption não sabe quais modelos o projeto usa, e um framework que preenche uma lista default é exatamente o que a regra de neutralidade de provedor existe para impedir — um placeholder visível falha alto na primeira execução; uma preferência inventada não falharia. 6 testes, cada um validando contra o schema real do repositório | 0 ✅ |
| GAP-151 | Validador de JSON Schema é subset incompleto — **corrigido** | HIGH | Além do `additionalProperties` já resolvido no GAP-132, **`type` como array era ignorado**: `"type": ["boolean", "string"]` — a forma que significa "uma flag pode ser bool, ou um caminho para o valor que a sobrepõe", usada em todo o schema — não era checado. **`enum` só era lido como `[]any`**, então um enum verificado contra um valor que não passou por JSON não restringia nada. E `matchesType` aceitava apenas `[]any` e `map[string]any`, o que fazia qualquer valor Go montado em memória reprovar em todo array e objeto — e a resposta tentadora a isso é afrouxar o schema, convertendo um bug de validador num contrato que não significa mais nada. Tudo por reflexão, e `[]string` continua reprovando quando deveria. 3 testes | 0 ✅ |
| GAP-152 | Ajuda anuncia comandos inexistentes — **corrigido** | MEDIUM | A ajuda listava `adopt scan`, `adopt facts`, `adopt classify` e `adopt scaffold` — **nenhum existia**. `adopt` aceita flags, não subcomandos, então cada exemplo era um comando que imprimia erro de uso. E o `compile` anunciava o target `claudecode`, enquanto o alvo real é `claude-code`. Ajuda que mente é pior que ajuda rala: é o primeiro lugar onde qualquer um olha. A lista documenta a superfície real — as nove flags, e `--apply` e `--audit-only` entre elas. 4 testes, um deles rodando **todo** exemplo do registro contra o parser real, e um exigindo que todo target documentado seja suportado | 0 ✅ |
| GAP-153 | Eventos de escrita em disco O(n²) — **corrigido** | HIGH | A rotação rodava a cada append, e `RotateLog` abre e lê o log inteiro para decidir se precisa aparar — fazia isso para todo evento, incluindo a imensa maioria que não precisava de nada. **Precisão sobre a forma**: o log tem teto, então cada leitura é limitada e o total é linear, não quadrático como o gap dizia. O que o tornava caro era uma leitura de arquivo inteiro paga por evento. Medido em 20k eventos com tudo o mais idêntico, ler por evento custa 12.6s e contar custa 0.6s, para os mesmos 240KB de log. Contar é O(N) amortizado, e a contagem volta para a metade que o arquivo passa a ter. Minha primeira versão voltava para o teto, o que rotacionava a cada evento e media 56x mais lento que o código que substituía; o benchmark foi quem pegou isso. Commit `843e0cd` | 2 ✅ |
| GAP-154 | `RecordDiff` reescreve tudo sob lock global — **corrigido** | HIGH | `saveDiff` segurava o `Server.mu` global durante read, marshal, write e rename. `opList` usa o mesmo lock para ler o mapa de runs, então cada diff gravado bloqueava listagem, status e início de run pelo tempo da escrita em disco. O diff store tem lock próprio agora: mapa de runs e disco são recursos diferentes. O `run_id` também é validado, como nos outros caminhos. Commit `220b95a` | 1 ✅ |
| GAP-155 | Tabelas de preço divergentes e sem versão — **parcial** | HIGH | A fonte de faturamento agora é única: `modelregistry/pricing.go` é versionada (`PricingVersion`), traz `EffectiveFrom` e `Source` por entrada, e é a que os providers usam para emitir `CostUSD` (GAP-129). As duas tabelas restantes **não são de faturamento**, e o register diz isso em vez de contar como a mesma divergência: `gateway.CostPer1k` é um único valor misto usado só para ordenar candidatos por preço no roteador (`gateway.go:91`), que ainda não tem caller de produção (GAP-102/103/108); e `budget/hierarchy.go:29-45` é uma terceira tabela em centavos BRL que **só o próprio teste usa** — nenhum código de produção a consome. Não as religuei: o que resta aqui é decidir conscientemente se a hierarquia entra no caminho real ou sai, e com que moeda, o que é decisão de produto e não de wiring | 2 🟡 |
| GAP-156 | Permissão-FW aceita privacy desconhecida no gateway — **corrigido** | MEDIUM | `t.Privacy != "" && t.Privacy != "local"` permitia valor vazio, então o caso em que **ninguém classificou** o target era justamente o que podia receber dado restrito. Um firewall que abre para "não sabemos" não é firewall: `LocalOnly` agora falha fechado e só aceita `local` explícito. Um valor de privacy que não é classe nenhuma também é recusado | 0 ✅ |
| GAP-157 | `EvaluateMCP` destrutivo é branch inalcançável — **corrigido** | MEDIUM | O adapter classificava por allowlist: um nome na lista read-only era ReadOnly e **todo o resto era SideEffecting**. `Destructive` — o tipo que o gateway veta outright num servidor não-confiável — não era emitido por nada, e o veto era um branch que nenhuma entrada alcançava. Um servidor expondo `delete_repository` era trancado como uma tool que renomeia arquivo. Agora a classificação é a do **próprio servidor**: `annotations.destructiveHint` e `readOnlyHint` do protocolo MCP são lidos, e uma tool sem hint continua SideEffecting — ReadOnly seria a direção permissiva de errar. Um operador também pode nomear tools destrutivas para servidores que não declaram nada, mas o default não é esse: um nome digitado num config é uma afirmação mais fraca que uma que o servidor faz sobre si mesmo. 5 testes | 0 ✅ |
| GAP-158 | 4 tools do registry sem dispatch — **corrigido** | HIGH→HIGH | O registry carregava uma lista escrita à mão ao lado do executor, e os dois divergiram: `read_file`, `write_file`, `exec_command` e `git_commit` estavam registrados com kinds, scopes, timeouts e output limits e **não tinham implementação nenhuma**, então `prumo tool list` anunciava capacidades que não podiam ser invocadas. Os descriptors agora derivam do catálogo ACI que os implementa, então uma tool que não está no catálogo não pode ser registrada e o registry não pode descrever algo inexistente. Isso fecha a classe, não as quatro instâncias. Entrada de catálogo sem mapeamento causa panic no startup em vez de omissão silenciosa, porque registry faltando tool é a mesma divergência na outra direção. Os verbos de análise foram mantidos e seus descriptors restaurados: cada um é implementado por um case em `runTool`. O teste da CLI fixava o false-green ao exigir os nomes fantasmas; agora exige os reais e que os fantasmas sumiram. Commit `54aba1b` | 3 ✅ |
| GAP-159 | Context de chamada descartado na ponte ACP | MEDIUM | `acp.go:29-47`, `56-86`; CLI passa `context.Background()` (`agent_commands.go:920-937`); `serveConn` não consegue ser cancelado (`acpserver/acpserver.go:117-147`) | 3 |
| GAP-160 | Lock de permissions não distingue ator — **corrigido** | MEDIUM | Toda aprovação era registrada com o ator `"client"`: um transporte, não uma pessoa. Um operador respondendo uma pergunta e quinhentos clientes Offered a mesma pergunta produziam registros idênticos, e nada no trail dizia quem tinha permitido um comando de shell. O daemon exige `fingerprint` desde o GAP-107 e o registro de ator não existia. O `actor` agora é aceito no op, enviado pelo SDK (`Client.Actor`) e pelo CLI, com fallback para a conta do SO no CLI. Um ator ausente é `"unknown"`, não `"client"` — "client" era uma fabricação; "unknown" é um fato sobre o registro. E um ator de 500 bytes não é identidade, é payload. 4 testes | 0 ✅ |
| GAP-161 | `AcquireLock` + release sem checar dono — **corrigido** | HIGH | `releaseLock` compara PID **e** StartTime antes de remover, e só remove se o lock ainda for o dele; se o daemon Apegou, o B fechou, e o A abriu de novo, o B não apaga o lock do A. O número de registro estava atrasado: o mecanismo já estava certo desde GAP-104, que trocou o pid nu por `LockRecord` com StartTime e BootID. Sem mudança de código | 0 ✅ |
| GAP-162 | `opList`/`opEvents` sem paginação — **corrigido** | MEDIUM | `opList` lia **todos** os records e devolvia todos; `opEvents` lia o arquivo inteiro do log para manter os últimos 500. Respostas ilimitadas para um cliente que pediu uma janela — um daemon que roda há um mês gastava a memória para mostrar os últimos vinte, a cada poll, por um socket. Agora ambos paginam por cursor estável (`after_id` + `limit`), e o log de eventos faz **seek** até o cursor em vez de ler o arquivo para descartar. Um cursor desconhecido devolve vazio com `has_more: false` em vez de reiniciar do começo — reiniciar reentregaria linhas que o cliente já viu e as chamaria de novas. `limit` tem teto, senão "um milhão" seria um jeito de pedir tudo mesmo assim. 7 testes | 0 ✅ |
| GAP-163 | Retenção não coleta run concluído — **corrigido** | MEDIUM | "Vivo" significava "tem checkpoint", e um run concluído preserva o checkpoint podado — então todo run que já terminou era permanentemente vivo e o coletor não coletava **nada**. Some-se que os prefixos `diffs-` e `daemon-run-` não estavam na lista, e diff é o maior artefato que um run produz: era justamente o que jamais seria coletado. A vivacidade agora vem do status no record do run. Um record ilegível conta como vivo — coletar os artefatos de um run que ninguém consegue prestar contas é como um run resumível vira não-resumível. 3 testes, um deles com um run concluído que **mantém** o checkpoint e mesmo assim é coletado | 0 ✅ |
| GAP-164 | Runs ativos não sobrevivem a restart — **corrigido** | HIGH | Um record dizendo `running` foi escrito por um processo que não existe mais. O daemon iniciava com mapa vazio e lia esses records como estavam: o status reportava uma run em voo quando nada a executava, e um cliente não tinha como distinguir run viva de run morta exceto esperando algo que nunca viria. `New` reconcilia o store antes de aceitar qualquer coisa. Run que parou esperando aprovação não está interrupted: está esperando, e o request está no checkpoint, então volta a `awaiting_approval` com seus pending ids — o step 6 do contrato de reconnect — e `restoreRun` reconstrói o runner atrás desses ids, com o trail de permissões carregado para não re-perguntar o que já foi respondido. Run em voo é marcada interrupted com o checkpoint de continuação, mas **não** é retomada: continuar gasta dinheiro que ninguém autorizou. Record terminal fica como está. Dois bugs achados pelos testes: `reconcileStore` derivava o run id do filename sem remover o prefixo `daemon-run-`, então toda lookup de checkpoint usava um id inexistente; e `RotateLog` não reportava se aparou, então um arquivo exatamente no teto (que não está acima dele) deixava o contador já reiniciado, um teto inteiro atrás do arquivo para sempre. Commit `dce1dda` | 2 ✅ |
| GAP-165 | Negociação de protocolo não é fail-closed — **corrigido** | HIGH | A negociação existia e nada a chamava: a versão que o cliente enviava nunca era lida, então um cliente construído contra outro protocolo era servido até alguma op se comportar diferente do que ele esperava. Todo request agora declara seu protocolo e o dispatch recusa quem não declara. Request sem versão é recusado em vez de assumido atual: um cliente velho demais para enviar versão também é velho demais para saber quais ops existem, então a ideia dele sobre qualquer uma pode estar errada. A recusa nomeia as duas versões, para o cliente ser corrigido em vez de adivinhar. O op `protocol` é isento — é como o cliente pergunta o que é suportado, e responder isso não pode exigir já saber. As três constantes de versão eram problema real: o SDK Go tinha "0.4.0" hardcoded, então um bump do engine deixaria o SDK negociando contra uma versão que ninguém implementa. Ele não pode importar a constante do engine (um client público compilando contra internal é inutilizável fora do módulo, e o teste de fronteira diz isso), então a cópia fica e dois guards prendem as três — um para o SDK Go, um lendo a declaração do client TypeScript, que é uma segunda cópia numa linguagem sem compilador para pegar a diferença. Commit `58ec76a` | 2 ✅ |
| GAP-166 | `remote` não implementa `subscribe` que o SDK anuncia — **corrigido** | MEDIUM | O handler local fazia caso especial para subscribe; o remoto despachava **toda** requisição e respondia. Um cliente que assinava por TCP recebia um resultado e o fim do stream: o método existia, era documentado, e não podia funcionar contra um daemon remoto. O loop de subscribe foi extraído e os dois handlers o compartilham. **Dois bugs que isso expôs**: o remoto escrevia no mesmo `bufio.Writer` a partir de duas goroutines sem mutex (o local tem um desde o GAP-119), e o `handle` local **não cancelava** a subscription ao fechar a conexão. 2 testes, um deles abrindo uma conexão TCP real, autenticando com token errado e depois certo, e exigindo mais de um evento do stream | 0 ✅ |
| GAP-167 | Fila de delegação sugerida pode não terminar — **corrigido** | HIGH | O teto era conferido só para `bounded-auto`, então `suggested` não tinha teto nenhum: um suggester que devolvesse sempre mais uma role crescia o time e o conjunto de resultados sem limite, e a única coisa que parava o loop era o suggester escolher parar. Além disso, repetir uma role não é progresso — produz o mesmo resultado. O teto vale para os dois modos, e um suggester que repete uma role é **recusado** com o nome na mensagem, em vez de ter a repetição descartada em silêncio: um suggester que acreditasse ter agendado trabalho que nunca rodou é pior do que um que foi avisado da recusa. Dois testes novos. Um teste existente foi corrigido e vale registrar por quê: o suggester dele devolvia sempre o mesmo nome e a asserção contava resultados, então media "role repetida não roda de novo" e chamava isso de teto. Passa a afirmar o contrato real — o suggester é consultado no máximo `MaxDelegations` vezes | 0 ✅ |
| GAP-168 | Worktree não é fronteira de segurança — **corrigido (limite declarado)** | CRITICAL | A delegação construía o executor com o mesmo construtor de uma run in-process, então uma run filha parecia tão contida quanto a mãe sem ser: uma tool call executava comando direto no worktree, e o `WorktreeProvider` — que se descreve como "git isolation only" — nunca era consultado. Nada registrava o que tinha acontecido. **A correção é tornar a fronteira dizível, não construir um container**: `aci.New` passou a declarar `SandboxNone` em vez de algo tranquilizador, porque o construtor recebe só um root e portanto não pode ter escolhido contenção; `NewWithIsolation` registra o `Kind()` do próprio provider, lido do provider e não passado à parte, porque um caller que nomeasse "container" para um provider local gravaria uma fronteira que não tem — e uma fronteira gravada é exatamente o que um leitor posterior confia. `RunnerDeps` ganhou `Isolation` e `RequireIsolation`, então um deployment que acredita que suas runs filhas são sandboxed tem como ser informado do contrário. Um worktree é um checkout git; continuar dizendo isso é o ponto | 1 🟡 |
| GAP-169 | Timeout por tool não existe — **corrigido** | HIGH | O tempo limite vivia só no registry de metadados do CLI: exibido, documentado, aplicado em lugar nenhum. A execução passava `context.Background()` ao comando, e sem deadline de run (GAP-122, agora corrigido) um `process.exec` pendurado segurava a run pelo tempo de vida do processo. A timeout é propriedade da tool, não do registry — por isso foi para o ACI, e o descriptor e o executor leem a mesma função. Um tool que não existe no catálogo não recebe deadline inventado: continua sendo desconhecido. 3 testes, um rodando `sleep 30` e provando que o corte acontece | 0 ✅ |
| GAP-170 | Teto de output aplicado depois de materializar — **corrigido** | HIGH | `os.ReadFile`, `CombinedOutput` e `bytes.Buffer` completam antes do corte: o teto limitava o que o modelo via, não o que o processo segurava. Um comando imprimindo dez gigabytes exhaustava memória antes de `bound` rodar, e o código lia como se o pedido fosse limitado. `fs.read` agora lê no máximo o teto mais um byte, e a saída de comando e de container passa por um buffer que guarda o teto e conta o resto — contando o que foi descartado, porque um teto que absorve o descarte em silêncio é um teto que ninguém pode auditar. **Um erro meu, pego pelo teste**: a primeira versão da cap calculava a truncagem e jogava fora, então toda saída grande voltou marcada como não truncada. 3 testes | 0 ✅ |
| GAP-171 | Gateway decide rota no primeiro evento do stream — **corrigido** | HIGH | `streamWithRetry` aceitava a rota assim que o primeiro evento chegasse, sem importá-lo. Um provider cujo stream abre com preâmbulo era considerado bem-sucedido antes de poder falhar: o CLI do opencode emite `step_start` e só depois o 404 de um modelo que não serve, então o gateway se comprometia com uma rota morta e a falha chegava sem fallback sobrando. Descoberto por teste com dois modelos reais, não por leitura. Agora o gateway espera um evento que **comprometa** a chamada — texto, tool call, usage ou completion — bufferizando o preâmbulo em vez de descartá-lo | 4 |
| GAP-172 | Pool não aceita duas instâncias do mesmo adapter — **corrigido** | HIGH | `Register` chaveava por `p.Name()`, então duas instâncias do mesmo adapter colidiam: a segunda sobrescrevia a primeira e toda rota para aquele nome alcançava a sobrevivente. Com um provider é invisível; com um pool de dois — que é a razão de o gateway existir — defeats o propósito. Não é hipotético: o adapter opencode serve muitos modelos por uma implementação, então dois modelos são duas instâncias chamadas "opencode". `RegisterAs` registra sob uma chave de roteamento explícita; `Register` continua sendo o caso de provider único | 4 |
| GAP-173 | Fallback transfere o turno de um provider que roda suas próprias ferramentas — **corrigido** | HIGH | O guard de efeitos colava no flag `afterSideEffects` do caller, que é o flag de efeito **do Prumo**. Um provider que declara `ToolCalls: false` — o opencode — roda o próprio loop e pode ter mudado o mundo a partir do momento em que começa, mas para o Prumo esse flag é sempre falso, porque nenhuma ferramenta do Prumo foi executada. O caso que mais precisava do guard era exatamente o caso cego dele: o turno ia para o provider seguinte, que recebia uma conversa sem registro do que o primeiro fez, e replanejava. Um provider que falhou *antes* de emitir qualquer coisa não pode ter tido efeito, e negar o fallback ali deixaria um provider inalcançável derrubar a run inteira — então o guard deriva do que o provider disse sobre si mesmo e de ter começado ou não. `DelegateWorkInFlight` dá ao runtime um sinal tipado, e o `StopReason` diz que o trabalho delegado não está contabilizado e precisa ser inspecionado antes de retomar. Descoberto ao implementá-lo, não por leitura | 4 |
| GAP-174 | Contrato de tool-call não carrega estado opaco do provider — **corrigido** | HIGH | O `ToolCall` não tinha onde guardar o token que alguns providers emitem junto de uma chamada e exigem de volta no turno seguinte. O caso é o `thoughtSignature` do Gemini: uma parte de `functionCall` carrega alguns centenas de bytes de base64, e uma conversa que reproduz a chamada sem ele é recusada com **400** — não degradada, recusada. Um adapter que parseia a chamada e descartava a assinatura pareceria correto no primeiro turno e falharia em todos os seguintes, e um teste que só checa a primeira resposta chamaria isso de sucesso. O campo é um mapa opaco com chave do adapter, que o core nunca lê: nomear o campo pelo provider colocaria o protocolo de um vendor no core neutro, e um campo único não sobreviveria a um segundo provider com token próprio. Persistir não é opcional — uma run retomada que perdesse a assinatura falharia no primeiro turno de tool, que é a mesma falha com outra causa | 4 |

## 2. Definition of Done (§35) — estado por item

> **Reavaliado em 2026-09-23** contra a auditoria de implementação. Itens que
> dependem de biblioteca sem caller de produção caíram para `🟡 partial`.
> O veredito de split (§3) mudou de "READY" para "NÃO READY".

| # | Item DoD | Status |
|---|----------|--------|
| 1 | docs reconciled/promoted | ✅ |
| 2 | AgentRuntime contracts | ✅ |
| 3 | FakeProvider conformance | ✅ |
| 4 | ≥1 real ModelProvider works | ✅ código+discovery (live: GAP-038) |
| 5 | NativeAgent end-to-end | 🟡 | run único funciona; `resume` não continua trabalho (GAP-123) |
| 6 | tools via ToolGateway | 🟡 | executor direto; 3 side-effect engines órfãos; 4 tools sem dispatch (GAP-126/158) |
| 7 | permission lifecycle | 🟡 | não recarrega após restart; identidade por call-id (GAP-106/107) |
| 8 | Environment/Sandbox baseline | 🟡 | symlink escapa do root; `--safe` não existe no agente (GAP-110) |
| 9 | checkpoint/restart/resume | 🟡 | lock não-atômico; resume marca Complete; run ativo não sobrevive (GAP-104/123/164) |
| 10 | duplicate side effects prevented | 🟡 | journal não está no caminho real; `pending` reexecuta (GAP-126) |
| 11 | budget enforcement | 🟡 | CLI liga; daemon cria tracker ilimitado; cobrança pós-resposta (GAP-001/100) |
| 12 | observability/events | 🟡 | dual-write existe; IDs colidem; escrita é O(n²) (GAP-118/153) |
| 13 | gateway routing/fallback | 🟡 | gateway sem caller; circuito não recupera (GAP-102/103) |
| 14 | ≥1 external AgentProvider works | ✅ nos limites (send: GAP-039) |
| 15 | typed Handoff | ✅ |
| 16 | Context Compiler v2 baseline | ✅ repo-map+LSP+FTS+Atlas (graph futuro) |
| 17 | Knowledge Runtime baseline | ✅ (Atlas: GAP-018) |
| 18 | KnowledgeDelta validate/commit | ✅ Delta-first + ledger (GAP-019/026) |
| 19 | Coverage/Readiness baseline | ✅ |
| 20 | Documentation Compiler baseline | ✅ regions+patch (GAP-007) |
| 21 | multi-agent/worktree baseline | 🟡 | binding e merge existem e testam, mas sem chamador de produção; worktree não é fronteira de segurança (GAP-003/168) |
| 22 | compatibility/eval suite | ✅ +kill-test+benches+TS (GAP-012/014/041) |
| 23 | `prumo agent` headless usable | ✅ | CLI canônico é `prumo` de `cmd/prumo` conforme ADR 016; TUI em `prumo-tui/cmd/prumo-tui` (GAP-139 é drift de doc/CI, não do binário) |
| 24 | docs describe reality | 🟡 | README e workflow de CI ainda na nomenclatura do ADR 015, superseded pelo ADR 016 (GAP-139) |
| 25 | no P0 contradictions | ✅ (contradição não-P0 da regra de imports do `cmd` resolvida por ADR 005) |

## 3. Split gate — estado por item

| Item | Status |
|------|--------|
| HA0 contracts | ✅ |
| HA1 FakeProvider + first ModelProvider | ✅ |
| HA2 Native Agent | ✅ |
| HA3 Tool/Permission | ✅ |
| HA4 checkpoint/restart/resume | ✅ |
| HA5 ACI + Sandbox baseline | ✅ c/ limites |
| headless coding Run end-to-end | ✅ |
| versioned public protocol | ✅ (IDL + SDK Go + SDK TS com roundtrip live: GAP-041) |
| reconnect/replay | ✅ local + remoto TLS c/ token (GAP-030 nos limites: CA corporativa) |
| ADRs do split | ✅ ADR 005–009 escritos (005/006/008 Accepted; 007/009 Proposed pendentes de medição) |
| **Veredito** | **NÃO READY (2026-09-23)** — a auditoria de implementação rebaixou 7 gaps com `done` falso e abriu GAP-097–170. Bloqueadores para READY: GAP-097 (comando que falha reporta exit 0), GAP-123 (resume marca Complete), GAP-126 (journal fora do caminho real), GAP-102 (gateway sem caller), GAP-101 (delegação sem caller). Até a Onda 0 e a Onda 2 fecharem, o split gate não pode ser declarado condicional-pronto |

## 4. Fora deste Goal (não entra na conta)

Desktop/TUI (prumo-code), execução cloud/microVM (H18), federação A2A (H17), site público/i18n (HD5+), polish visual, `prumo-code` em si.

> Nota (2026-09-17): a integração do cliente expôs duas lacunas de vocabulário, ambas
> fechadas no core: o timeline passou a carregar conteúdo do modelo (`text_delta`,
> `reasoning_delta`, `tool_call_ready`) e a reportar mudanças de arquivo
> (`file.changed`, com caminho e operação, vindos do catálogo ACI pelo port
> `ToolExecutor.OperationOf`). Um run que apenas parou para decisão emite `run.paused`,
> não `run.finished`.
>
> Nota (2026-09-17): o cliente do spike H10 foi arquivado em `archive/tui-poc/` e o
> cliente do produto passa a ser `prumo-agent tui`, sobre a base OpenCode Go — decisão em
> **ADR 013**. As provas de GAP-046/GAP-047 seguem válidas sem alteração: o que elas
> fixaram (as ops de aprovação, o estado `awaiting_approval`, o baseline de redraw)
> vive no protocolo e nos contratos, não no cliente arquivado.

## 5. Documentation Control Plane Gaps (DOC-GAP-001 – DOC-GAP-033)

Source: `PRUMO_DOCUMENTATION_CONTROL_PLANE_DEEP_AUDIT_AND_WAVES.md` (2026-09-14,
artefato de entrada gitignored). Mapeados para as Waves W0–W21 em
`docs/development/waves.md`. Decisão arquitetural: ADR 010 (Proposed).

> Nota de status: o fix **léxico** imediato do DOC-GAP-002/003 aterrissou em
> `internal/documentation/coverage.go` em 2026-09-14 (matching por todas as
> palavras-chave + stopwords; evidência distinta e não-vazia) com regressão
> `TestFalseGreenKnowledgeMatchingPrevented` / `TestEvidenceDistinctAndNonEmpty`.
> Em 2026-09-15 o alvo chegou: `internal/documentation/semantic.go` implementa
> requirement → claim → evidence e o avaliador léxico foi **rebaixado a
> diagnóstico** (`mode: lexical`, `authoritative: false`, estado máximo
> `unverified`). W15.10 também aterrou: os 7 contratos do próprio repositório
> carregam claims aceitas com evidência verificada e `prumo-agent docs readiness`
> reporta `ready: true` sem warnings. Vale registrar **por que** demorou: a
> primeira versão dos bindings citava `docs/migration/conformance-strategy.md`,
> que o authority map classifica como **historical**, e o avaliador rejeitou como
> `non-canonical-evidence` em vez de aceitar o verde. Foi o caso D002 do corpus
> léxico (misbinding de evidência) disparando no repositório real.
>
> Em 2026-09-15 as waves W17–W21 também aterraram: impacto semântico tipado e
> preflight/postflight de Goal (W17), plano de publicação e recuperação por IA
> (W18), verificador determinístico, gate `--strict` e Gauntlet (W19), ciclo de
> vida de tradução/mídia/release (W20) e inteligência documental + adoção
> brownfield (W21).

| ID | Item | Tier | Wave | Status | Próximo passo |
|----|------|------|------|--------|---------------|
| DOC-GAP-001 | Authority/version drift não prevenido mecanicamente | P0 | W0, W1 | ✅ done | W0: `docs/governance/authority.md` + `docs/AUTHORITY_MAP.json` + `prumo-agent docs authority` (gate `docs-authority`); W1: `conformance/schema-runtime` (identidade, draft, enums, integridade referencial, round-trip) |
| DOC-GAP-002 | Semantic coverage/readiness excessivamente léxico (ANY-word bug) | P0 | W15 | ✅ done | `semantic.go`: requirement → claim → evidence; léxico virou diagnóstico não-autoritativo (W15.3–W15.7); corpus D001–D011 |
| DOC-GAP-003 | Verificação de evidência baseada em mera contagem | P0 | W15 | ✅ done | Evidência exige `id` estável, `target` = claim, revisão atual, `verified` e artefato existente; dedup por ID (`TestEvidenceDistinctAndNonEmpty`) |
| DOC-GAP-004 | Contradições entre documentos não barram completion | P0 | W15, W19 | ✅ done | W15: findings `contradiction` e `superseded-claim` bloqueiam readiness (D010/D011); W19: `VerifyDocsStrict` + Gauntlet contínuo sobre documentos |
| DOC-GAP-005 | Fiação Goal → DocumentationPlan incompleta | P0 | W17 | ✅ done | Preflight em Goal lock (`BuildPlanForContracts`), postflight em delta (`Reconcile`), `DocumentationGap`/`DocumentationPlan` populados em `internal/app/goalplan.go` |
| DOC-GAP-006 | Lint de HumanDocs valida apenas estrutura, não verdade/semântica | P0 | W10, W19 | ✅ done | W10: schemas de quality-policy e evidence-requirement; W19: verificador em camadas (authority → managed regions → links → claim-drift) + `semantic-readiness` sob `--strict` |
| DOC-GAP-007 | Instruções de agente como verdade paralela e obsoleta (context-rot) | P0 | W4, W16 | ✅ done | W16: IR canônico (`docs/agents/instruction-ir.json`) → adapters (agents-md/copilot/cursor/claude/skill-md) com fingerprint e orçamento de tokens; gate de context-rot (`prumo-agent docs agents verify` + `TestAgentSurfaceContextRotGate`) falha em referências obsoletas |
| DOC-GAP-008 | Análise de impacto não cobre tipos semânticos suficientes | P0 | W17 | ✅ done | `impact_semantic.go`: triggers por símbolo, schema, token, UI, API, evento, permissão, locale, mídia, Goal, Wave, ADR, release e evidência |
| DOC-GAP-009 | Pacotes de documentação fragmentados no runtime | P0 | W15 | ✅ done | Fronteira e direção de dependência definidas em `docs/architecture/documentation-control-plane.md`; composição no CLI, sem acoplar docengine ao harness |
| DOC-GAP-010 | Documentação "ready" diverge da verdade do código | P0 | W15 | ✅ done | `TestSemanticReadinessDogfood` + estado `unverified`; `prumo-agent docs readiness` não passa por palavras e hoje reporta `semantic=7 lexical=0` com `ready: true` porque os claims existem de fato |
| DOC-GAP-011 | Modelo DocumentationUnit necessita semântica mais rica | P1 | W3, W10 | ✅ done | `documentation-unit` schema completo (fontes, evidência, visibilidade, sensibilidade, locale, freshness, supersede, projeções, token estimate) |
| DOC-GAP-012 | Unidades de contexto e docs humanos não unificados | P1 | W3 | ✅ done | `knowledge-unit` + 13 relações tipadas + identidade estável; o baseline do Knowledge Runtime já semeia por IDs estáveis (`knowledge.DeriveID`), com manifest derivado (`internal/harness/knowledge/manifest.go`) e CLI `prumo-agent knowledge manifest\|search` |
| DOC-GAP-013 | Publicação HD5+ ainda deferida | P1 | W18 | ✅ done | `internal/docpublish`: interface de renderer, adaptador Starlight, rotas versionadas/localizadas, `prumo-agent docs site build\|verify` |
| DOC-GAP-014 | Ausência de projeções AI-native (llms.txt, MCP) | P1 | W18 | ✅ done | `/llms.txt`, `/llms-full.txt`, Markdown cru, índice de busca, referência de API/schema e recursos MCP de documentação: leitura sempre disponível (`prumo://docs/...`, `docs resources read`) e mutação negada por padrão (`docs mutations`, `allowed: []`) |
| DOC-GAP-015 | Documentação de UI/UX necessita ciclo de vida | P1 | W5, W20, H10 | ✅ done | W5 entregou os 16 contratos `ui.*`/`tui.*` e a evidência visual chegou em W20. **Correção de registro:** a entrada anterior afirmava que W5 havia entregado também "state matrix (22 estados) e component contract" — o artefato **não existia**, e ninguém percebeu porque os contratos `ui.*` não tinham binding e a capability `tui` nunca era composta, então nunca foram avaliados. `docs/ui-ux/state-matrix.json` foi escrito em H10 e agora é verificado por `TestStateMatrixIsCompleteAndExplicit`; o component contract segue dispensado com motivo em `bindings.json` (`ui.component-contracts`) |
| DOC-GAP-016 | Evidência visual sem semântica de frescor | P1 | W20 | ✅ done | `media-record` schema + `doclifecycle.MediaStatuses`: staleness por digest, ligada a estado/tema/locale/plataforma |
| DOC-GAP-017 | Fundação de tradução necessita ciclo de QA completo | P1 | W6, W20 | ✅ done | W6: lifecycle, locale-key, placeholders, reviewer, fallback, RTL; W20: `VerifyPseudoLoc` + staleness por digest de conteúdo |
| DOC-GAP-018 | Temas/personalização sem contratos explícitos | P1 | W9 | ✅ done | `design-token-set` schema + `docs/ui-ux/design-tokens.json` (tiers, temas no-color/high-contrast/reduced-motion, contraste medido, fallback TUI) + contrato `ui.personalization` |
| DOC-GAP-019 | Documentação de referência de API/CLI não orientada a contratos | P1 | W18 | ✅ done | `apiReferenceRenderer` gera as páginas de referência a partir de `docs/contracts/builtin.json` e de `schemas/*.schema.json`, marcadas como geradas (`Graph.Reference`) para nunca serem confundidas com páginas autorais |
| DOC-GAP-020 | Exemplos não são unidades de documentação executáveis | P1 | W19 | ⬜ open | Execução e teste de exemplos no pipeline |
| DOC-GAP-021 | Documentação de release/depreciação/migração sem ciclo estruturado | P1 | W20 | ✅ done | `docs/lifecycle.json` com release gates, `DeprecationFindings` (substituto ou remoção + anotação no documento) e `CheckRelease` → `prumo-agent docs release` |
| DOC-GAP-022 | Observabilidade documental subespecificada | P1 | W21 | ✅ done | `internal/docintel.Collect` + `prumo-agent docs metrics`: tokens, split léxico/semântico, traduções/mídia, links, claim drift, débito nomeado e `freshness_score` limitado |
| DOC-GAP-023 | Feedback de usuário/suporte não alimenta planning documental | P2 | W21 | 🚫 refused by design | Rastrear intenção exigiria um canal de telemetria; as regras de autoridade e trust do framework proíbem coletar o que usuários consultam. Decisão registrada em **ADR 011**; métricas vêm apenas do estado do repositório |
| DOC-GAP-024 | Workforce documental necessita especialização por impacto | P2 | W21 | ⬜ open | Roteamento dinâmico de skills por tipo de impacto (o impacto tipado existe em W17; o roteamento de workforce não) |
| DOC-GAP-025 | Segurança e visibilidade documental necessitam política de projeção | P2 | W21 | ✅ done | `Graph.Human()` vs `Current()` separa público/interno e superfícies de agente; a saída publicada é derivada, sob `.prumo/runtime/` |
| DOC-GAP-026 | Edição com AST completo de Markdown (listas/tabelas) | P2 | W21 | ⬜ open | Parser AST completo no doccompile |
| DOC-GAP-027 | Performance de build em monorepos/grandes repositórios | P2 | W21 | ✅ done | `TestLargeRepositoryIncrementalInvalidationIsBounded` (build frio escreve tudo, rebuild sem mudança não escreve nada, uma edição invalida um subconjunto) + `BenchmarkBuildLargeRepository` (200 docs: frio ~64,5 ms, inalterado ~14,5 ms; i7-3632QM) |
| DOC-GAP-028 | Adoção e migração brownfield de documentação | P2 | W21 | ✅ done | `Inspect`/`Propose`: sinais, inventário, duplicatas, propostas com confiança `inferred` e caminho de promoção; nada é escrito no repositório alvo |
| DOC-GAP-029 | Débito documental como classe de débito tipada | P2 | W21 | ✅ done | `documentationDebt` nomeia cada obrigação pendente; contrato não verificado sempre aparece como débito e reduz o score |
| DOC-GAP-030 | Rastreabilidade e explicabilidade no nível de consulta | P2 | W21 | ✅ done | `prumo-agent docs query` retorna rota/heading/excerto rastreáveis e `prumo-agent docs explain` explica unidade ou finding; a proveniência de consulta registrada (W21.2) foi recusada com DOC-GAP-023/ADR 011 |
| DOC-GAP-031 | Terminologia/glossário sem registro canônico | P1 | W20 | ✅ done | `docs/glossary.json` + `schemas/glossary.schema.json` + `internal/doclifecycle/glossary.go`: status fechado (preferred/deprecated/forbidden), termo deprecado deve nomear um substituto preferido, match por palavra inteira, `prumo-agent docs glossary`, e verificação dentro de `prumo-agent docs verify` |
| DOC-GAP-032 | Ausência de política explícita de versão documental | P1 | W20 | ✅ done | `version_policy` em `docs/lifecycle.json` + `schemas/doc-lifecycle.schema.json` + `internal/doclifecycle/versionpolicy.go`: buckets exclusivos (current/supported/deprecated/removed), drift contra `protocol.CLIVersion`, referência a linha removida reprovada salvo em documento histórico ou linha histórica anotada, `prumo-agent docs version` |
| DOC-GAP-033 | Telemetria de consulta/intenção documental | P2 | W21 | 🚫 refused by design | W21.2/W21.3 não implementados: não existe canal de telemetria e registrar consultas armazenaria conteúdo sensível por construção. **ADR 011** registra a decisão, o limite e a condição para revertê-la |
